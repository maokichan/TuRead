# 渲染层风格基线（Style Baseline v0.1）

> 归属：**client 专属**。权威范围：`client/src/renderer/**`（UI 外壳 + 展示组件 + `styles.css`）。
> **不约束** core（domain / ports / usecases / adapters）——风格是外壳的事，端口契约见 `CONTRACTS.md`。
> 状态：**v0.2 生效中（2026-09-09）**。本文是渲染层开发的**准则**：新组件先读本文，再动代码。
> 冲突裁决顺序：本文 > `FEATURES.md`（功能划分）> 组件内注释。改本文 = 契约先行，同 commit 更新。
> 登记位置：`MAP.md` 关键文档 §13；红线「渲染层开发先读本文」。

---

## 1. 风格定性

**文字即界面（typographic chrome）**：界面用"排印过的文字"承担导航、动作与状态，
把字体（而非边框、图标、色块）作为主要的视觉语汇。

三个来源，缺一不可：

| 来源 | 提供什么 | 本项目的落点 |
|---|---|---|
| 排版优先（iA Writer 谱系） | "chrome 可以不存在"的哲学；用一套字体解决专注 | 无顶栏/无 logo/无菜单栏；文字按钮 |
| 瑞士国际主义（栅格 + 层级） | 层级靠字号/字重/留白，而非颜色与装饰 | 灰阶 token + 单一 accent |
| 中文排版规范（赫蹏 / jlreq） | 汉字与西文混排、标点、行距的**硬规则** | §4（本基线的核心增量） |

> 现状（v0.1.11 收口后）：书库/阅读器/房间/设置的动作入口已统一为文字按钮（`.text-action`），
> 边框只剩输入类与浮层两类例外；本文的目的从"消灭两套语汇"转为"守住一套语汇"。

## 2. 五条原则（冲突时按序裁决）

- **P1 文字优先**：功能入口、动作、状态**优先**以排印过的文字呈现。边框/图标/色块是**例外**，
  必须在 §5 的例外清单里列明；新增例外要改本文。
- **P2 一套字体**：界面 chrome 只用**一套字体**（西文 Times New Roman + 中文源流明體，§3.1）；
  **阅读器正文由 kookit / 电子书决定，基线不干预**。字体不承担层级，层级交给 §3.2 的字号。
- **P3 层级靠排版**：字号 → 字重 → 字距 → 行距 → 留白。颜色只做"**一次**强调"，不承担分类。
- **P4 内容优先**：chrome 可消失。不设常驻顶栏/logo/状态栏（诊断日志在设置里）。
- **P5 动效克制**：一屏一动作，同一时刻至多一个关键帧，时长 ≤ 650ms，无弹跳/无缩放。

## 3. 令牌（唯一来源 = `styles.css`）

**组件内禁止写死 hex/rgba/字体名**，一律走 token 或 Tailwind 的 `var(--*)` arbitrary value。

### 3.1 字体（2026-09-09 起：**全局统一**）

**界面 chrome 只有一套字体**：西文/数字 = `Times New Roman`（印刷体），中文 = 源流明體（打包，OFL 1.1）。
实现：`--font-ui: 'Times New Roman', 'GenRyuMin TW', …`，`body` 继承；`--font-serif-cn` 为同值别名。

> ⚠ **顺序不可反**：Times New Roman 无 CJK 字形 → 汉字自动回退到源流明體；
> 若把源流明體放前面，它子集里的拉丁字形会抢走西文。
> ⚠ **阅读器正文不在此约束内**：kookit 在 iframe 内自建样式，宿主字体不会继承进去（§4.3）——
> 电子书的字体/字号由引擎与书本身决定，基线只保证"不改它"。
> ⚠ 源流明體只有 Bold 一个切面 → 中文**不能做字重对比**，层级只能靠字号/字距/颜色。

| 声部 | 字体 | 用在哪 |
|---|---|---|
| 统一界面字体 | Times New Roman + 源流明體 | 全部界面文字：导航、按钮、标题、字段、状态、日志 |
| 阅读正文 | 由 kookit / 电子书自身决定 | 阅读页 iframe 内 —— 基线**不干预** |

> 原"三声部"（衬线 / 无衬线 / 等宽）已按用户决定收敛为**单一字体**。
> `--mono` 保留为**同值别名**（避免大范围改动）；若要恢复"技术数据等宽对齐"，
> 把该 token 改回 `monospace` 栈即可——但那时要重新评估与 P2 的关系。

### 3.2 字号阶梯（px，白名单外禁止）

`11 · 12.5 · 13 · 14 · 15 · 17 · 18 · 19 · 22`，以及封面自适应 `10–120`（`FittedTitle` 二分拟合）。
`22` 仅用于书库底部状态栏的两个文字按钮（`.text-action--lg` / `.view-switch`）。

| 用途 | 字号 | 声部/字重 |
|---|---|---|
| 技术数据、辅助说明 | 11 / 12.5 | 400 |
| 表单、正文控件 | 13 | 400 |
| 面板标题 | 14 / 15 | 700 / 600 |
| 列表标题 | 17 | **700 但视觉不加粗**（现状，见 §5.4） |
| 动作文字按钮 | 18 | 700 |
| 侧边栏符号 | 19 | 700 |
| 状态栏文字按钮 | 22 | 700，字距 `0.08em` |

### 3.3 字距 / 行距 / 留白

| 项 | 基线 |
|---|---|
| 字距 | 中文界面 `0`；文字封面 `0.01em`；小标签/大写标签 `0.6px`；状态栏文字按钮 `0.08em` |
| 行距 | 界面 `1.35–1.6`；文字封面 `BASE_LEADING 1.02` → 余量分给行距，上限 `2.4` |
| 留白刻度 | `4 / 8 / 12 / 16 / 20`（Tailwind `1/2/3/4/5`），不出现奇数间距 |
| 圆角 | 小控件 `rounded`；行/按钮 `rounded-lg`；面板 `rounded-xl` |
| 阴影 | **无阴影**（抽屉已于 2026-09-09 去掉 `shadow-2xl`）；层级靠底色与留白 |
| 动效 | 全应用**唯一**动画 = 视图切换 900ms（`view-switch-flash`）；**动画期间按钮 disabled**（防连点导致动画不出现） |

### 3.4 颜色

- 基底**灰阶**：`--bg / --panel / --panel-2 / --border / --border-soft / --text / --muted`。
- **单一强调**：`--accent`（+ `-soft/-strong/-ring/on-accent`）。强调只用于"当前选中 + 主动作"。
- **语义状态色**：`--ok / --warn / --err`（+`-border`）——只表达状态，不做装饰。
- **主题模型（2026-09-11 规正，权威 `core/domain/theme.ts`）**：**2 主题 × 2 模式** ——
  主题（tone）＝ 纯色（黑灰白）｜羊皮纸（暖棕）；模式（mode）＝ 深｜浅。数据层沿用四值
  `dark=纯色·深 / light=纯色·浅 / sepia-light=羊皮纸·浅 / sepia-dark=羊皮纸·深`。
  四套 token 组（`styles.css`）：`:root`＝纯色·深，`[data-theme=light|sepia-light|sepia-dark]` 各一套。
  「跟随系统」只作用于**模式**，在**所选主题内**解析。**新增取向 = 复制整套 token**，不允许局部覆盖。
- **阅读页跟随模式**：`--page-bg/--page-text` 是正文页的纸色 —— **深色模式必须用该主题的深色纸**（纯色·深＝深灰纸），
  不再"暗色主题仍白纸"（2026-09-11 改，用户定：夜间模式与深/浅选择挂钩）。
  非 PDF 正文的深色渲染由 `IRenderService.applyTheme` 注入（`CONTRACTS.md` §4.1）；浅色不注入、保留书的外观。
- **阅读页的"桌"与"纸的边"（2026-09-11 v0.5，四套主题各一组）**：`--desk-bg`（纸外背景）+
  `--page-edge`（纸的 1px 边）。**内容与背景靠颜色区分**——桌比纸暗一档，纸再有边线；
  不用阴影/圆角。新增取向时这两项随整套 token 一起复制。
- **阅读页布局尺寸（同一处登记，避免多处硬编码漂移）**：`--sidebar-w`（侧边栏宽度，AppShell 与目录几何共用）、
  `--read-width`（正文列宽，由「设置」写到 documentElement）、`--page-pad-x`（**纸内边距** = 正文到纸边的距离，
  由适配器注入 `body{padding-inline}`）、`--toc-line-w` / `--toc-line-top`（挂载线长度与位置）、
  `--toc-veil-rest`（目录静息遮罩不透明度，越大越淡）。
- **负片（反色矩形块）**：`--negative-bg: var(--text)` / `--negative-text: var(--bg)` ——
  派生自各主题的 text/bg，所以四套取向自动成立。语义 = 文字块成为"纸的反面"
  （页面白 → 块黑字白）。**只允许详情抽屉的文字块使用**（§5.5）。

## 4. 中文排版（本基线核心增量）

> 此前源流明體只被当"好看的标题字体"用，汉字与西文混排、标点、段距**没有任何规则**。
> 参照：[赫蹏 heti](https://github.com/sivan/heti)（中文排版规范实现）、
> [W3C jlreq](https://www.w3.org/TR/2012/NOTE-jlreq-20120403/ja/)（日文组版要求，CJK 通用）、
> [Typotheque · CJK 組版](https://www.typotheque.com/articles/typesetting-cjk-text-jp)。

### 4.1 硬规则

1. **中西混排间距**：汉字与西文/数字之间应有约 `1/4 em` 空隙。
   优先用 CSS `text-autospace` / `text-spacing-trim`；**浏览器不支持时不做处理**，
   **禁止**用 JS 往文本里插空格（污染数据、破坏复制与搜索）。
2. **标点**：中文用全角标点；数字/英文用半角（不写全角数字）。
   行首禁则（`、。，）」` 等不出现在行首）、行尾禁则（`（「` 不出现在行尾）：
   用 `line-break: strict` + `word-break: normal`；`text-spacing-trim` 支持时启用挤压。
3. **行宽**：中文正文 **28–40 字/行**（超出即增大容器内边距或收窄版心）。
4. **行距**：中文正文比西文松——`1.6–1.8`；界面文字块按 §3.3。
5. **段距 vs 首行缩进：二选一，不得并用**。
   本项目默认 **段间距、不缩进**（界面与阅读页一致，避免"半格缩进"错觉）；
   若将来提供"纸书排版"档，才启用 `text-indent: 2em` 且段距归零。
6. **纵排预留**：容器抽象不得假设横排（避免把宽度写死成"字数 × 字号"）；
   `writing-mode: vertical-rl` 的实装**不在 v1**，但新组件不得阻断它。

### 4.2 落地位置

所有中文排版规则集中在 `styles.css` 的一个区块（`.cjk-body` / `.cjk-ui` 两个类），
**组件不自行设置** `letter-spacing` / `text-indent` / `line-break`。

## 5. 组件规则

### 5.1 必须用文字（P1 默认）

- 导航：侧边栏、视图切换、标签页切换。
- **侧边栏选中态 = 负片块**（`.feature-nav--active`，反色），**不用圆角按钮/底色/描边**
  （2026-09-09 用户定）；未选中 = 单色汉字，hover 只变色温。
- **列表类导航一律是纯文字行**：阅读器目录（`TocPanel`）、房间大厅（`RoomRow`）——
  无边框、无底色、无圆角、无分隔线，靠**留白分行**；hover 只做色温变化。
- 主要动作：導入、发送、打开阅读、创建房间、进入。
- 状态与计数：`3 本`、`提取封面 2/5`、连接状态、房间号。

**主/次动作的区分靠色温，不靠边框**：主动作 `--accent`，次动作 `--muted` → hover 升为 `--text`。
样式统一定义在 `styles.css` 的 `.text-action` / `.text-action--primary` / `.text-action--danger`，
**组件只挂类名**，不得自行写字号/颜色。

**三角负片标记（唯一例外）**：只有**书库底部状态栏的视图切换按钮**带右下角三角
（`.view-switch::after`，`mix-blend-mode: difference` 的**真负片**）。
它**压在文字本身上**（不是贴在按钮外侧）——压到的字形会被反相。
理由：该按钮的职责是**引导用户点击切换视图**；其他文字按钮不需要。
新增带三角的按钮必须先改本文。

**状态栏布局（2026-09-09 定）**：底部状态栏**所有元素统一左对齐**（不再左右对称分布），
分割线随内容区宽度铺开，`pt-4` 让分割线相对内容区上移。
两个文字按钮放大到 22px + 字距 0.08em（`.text-action--lg` / `.view-switch`）。
**不显示书目数量**（无关紧要，2026-09-09 用户定）。

**按钮盒必须紧贴文字**：`.view-switch` 用 `padding: 0` + `line-height: 1.1` + `width: fit-content`。
原因：动画的背景块画在按钮盒上 —— 盒若被 padding 撑大，文字就"填不满背景"，
三角标记也会落到字外面（2026-09-09 修）。**任何带背景动画的文字按钮都要遵守这条。**

### 5.2 允许边框/底色的例外（必须在此列明）

| # | 例外 | 理由 |
|---|---|---|
| ① | **输入类**：`input` / `textarea` / 下拉 | 可点区域必须可见，文字无法表达"此处可输入" |
| ② | **破坏性动作**（移除、断开、删除）：文字形态 + `--err` 色 | 仍是文字按钮，**只允许用语义色**，不加红框红底 |
| ③ | **浮层**（详情抽屉、导入菜单、**阅读器侧边栏贴缘按钮**） | 需要与内容区分层级。**抽屉已于 2026-09-09 去掉边框/阴影**（§5.5），只剩「导入菜单」保留浮层边框；**目录挂载列表容器全透明**（§5.8，2026-09-11 改） |
| ④ | **状态胶囊**（`StatePill`） | 语义色点 + 文字，属于 §3.4 的状态色 |

> 例外之外若想加边框，先改本文。

### 5.3 图标与符号

- **只允许**侧边栏的汉字单字符号（書/閱/房/設），单色，衬线体。
- **禁止**彩色 emoji、**禁止**引入图标字体或图标库（新增依赖须先核许可证与 AGPL 兼容性）。

### 5.4 现存的已知偏差（待收敛，逐条改）

| 偏差 | 位置 | 处置 |
|---|---|---|
| 列表标题 17px 用 `font-normal` —— 源流明體只有 Bold 切面，浏览器会取该切面（视觉仍偏粗） | `BookRow.tsx:83` | 记为**唯一豁免**（其余文字块不写 `font-normal`） |
| 输入框/表单仍保留边框 | `RoomFeature/index.tsx`、`SettingsFeature` | 属 §5.2 例外①，**保留**（输入类必须可见可点） |
| 浮层容器保留边框/底色 | 导入菜单、确认弹窗、连接卡片 | 属 §5.2 例外③，**保留**（内部按钮与菜单项已是文字） |

### 5.5 负片（反色矩形块）—— 仅两处

**用途（2026-09-09 定）**：① 书库详情抽屉的标题与字段；② **侧边栏选中项**（`.feature-nav--active`）。

**抽屉（`BookDetailPanel`）—— 平面结构（2026-09-09 用户定）**

```
┌────────┬───────────────────────────────┐
│        │ ① 标题条（负片，过长自己滚）    │  ← 三行 = 封面高度三等分（32px×3），
│ 封面    │ ② 数据行（格式·大小·时间·路径）  │     左对齐「封面右缘」，撑满抽屉宽度
│ 64×96  │ ③ 指标行（已读 42%｜3 天前｜共 —）│
├────────┴───────────────────────────────┤
│ 描述块（满宽负片）                        │
└────────────────────────────────────────┘
```

- **封面保持 2:3 原始比例（64×96）**：三行各 32px、合计正好等于封面高度 ——
  **不得为了排版把封面拉伸/裁成细条**（2026-09-09 教训：`w-[72px]` + `items-stretch`
  把封面拉成 72×172，`object-cover` 裁得只剩中间一条）。
  需要更多横向空间时**调抽屉宽度**（现 280px），不要动封面比例。
- **不给任何容器加标签**（2026-09-09 用户定）：内容自己说明性质 ——
  "已读 42%" 不必写"阅读进度"，"3 天前" 不必写"上次阅读"，"共 —" 是总阅读时长占位。
  因此上一版的竖排标签（`.vertical-label`）已删除。
- **单行硬约束**：① ② 用 `Marquee`（超出才滚，绝不换行向下生长）；文件路径并入 ②。
- 外壳**完全透明**（无边框、无阴影、无底色），下层书库内容从文字块之间透出来。
- 动作按钮用 `.text-action`；**关闭按钮在底部**（Esc / 点抽屉外是主要关闭路径）。
- 占位待实现：总阅读时间、描述内容（均见 `TODO.md`）。

> 为什么不做"全应用负片"：负片是高对比的强调手段，用多了会失去层级（P3）。
> 若要扩大使用范围，先改本文并重新评估对比度。

### 5.7 设置页的分组结构（2026-09-09 定）

设置页**不使用圆角容器**（设置项会持续增加，容器会变成一堆盒子）。
分组 = **副标题（14px 700）+ 上分割线**（`Field` 组件，`border-t` + `pt-4`）；
说明文字用 11px `--muted`。诊断日志同样用分组标题，日志块只保留底色、无边框无圆角。

### 5.8 阅读器沉浸态（准则；历史见 §10 修订记录）

**原则（用户定，对应 koodo 式阅读）**：进入阅读 = 专注文本。**不必要的内容全部退场** ——
无常驻状态栏、无无用功能键、无圆角边框的"嵌入盒子"；界面上只保留需要渲染的文本内容。
两条容易做错的地方先写在最前面：**全屏的是"桌"不是"正文"**；**鼠标一动就浮出来的栏同样是状态栏**。

| 规则 | 内容 |
|---|---|
| **纸与桌** | 全屏铺开的是**桌**（`--desk-bg`）；正文在**居中的定宽「纸」**里（`--read-width`，px；档位 窄 620 / 中 760 / 寬 920，默认 **760**）。**内容与背景必须可分，而且靠颜色**（不是阴影/圆角）：桌比纸暗一档（四套主题各一组），纸再有 **1px `--page-edge` 边** |
| **纸的内边距** | `--page-pad-x`（正文到纸边的距离；档位 24/44/72）—— 适配器注入正文 `body{padding-inline}`。这是"出血"唯一的正确加厚位置（见 §5.9 表 #6 的算术约束） |
| **宿主容器 = 纸** | 滚动宿主仍是 `#page-area`（硬契约，`KOOKIT.md` §5.1/§5.8），但它现在是**纸**（定宽居中、`overflow-y:auto`），外层是**全屏桌**容器。纸宽 = kookit 的排版宽度依据（宿主 `clientWidth`）→ 改纸宽即改"一行多长"、图片缩放与分页宽度，**不需要新端口**。两侧留白 = 未来右侧容器的可增长区（§5.9） |
| **滚轮** | 落在两侧留白上也要能滚：外层桌把 wheel 转发给纸（否则鼠标停在留白上没反应）；纸内部走原生滚动，转发要跳过，否则双倍 |
| **滚动条** | "必要的器"而非装饰，**不得在用户不希望的时候影响注意力**：**2px**；静息只有 `--page-text` **10%**，**指针进入纸内**才升到 26%；**短章节整条隐藏**（判据见下）。⚠ 只写 `::-webkit-scrollbar`，**不能写标准属性** `scrollbar-width`/`scrollbar-color` —— Chromium 121+ 起写了它们就会让 webkit 规则被**整个忽略**（实测量出来是 `thin` 的 ~11px）。桌容器 `overflow:hidden`，滚动条不会跑到屏幕边缘 |
| **短章节的判据** | kookit 给文字类 iframe 的 `height` 硬留 **+300px 余量**（`handleIframeHeight`），所以"短章节也总有得滚"不是真溢出。溢出 ≤ 余量时适配器设内联 `scrollbar-width: none`（**完全不占位**：标准属性优先正好当开关用）；真溢出才让 2px 规则生效 |
| **阅读页零控件** | 书名、页码、上一页/下一页/目录/关闭**都不常驻、也不做"浮现栏"**。进度与位置不占操作面 |
| **退出** | `Esc`（键盘）；鼠标路径 = 贴缘按钮召回侧边栏 → 「書」。**不为退出在阅读页上加按钮** |
| **翻页** | 键盘 `←/→` `Space(分页模式)` `PgUp/PgDn`；鼠标点击左右 1/3 区域（仅分页模式）；scroll 模式滚轮。**目录开合 = `t`**，**阅读参数 = `p`**（键盘，无界面痕迹） |
| **侧边栏退场（覆盖式）** | `activeFeature === 'reader'` 时左侧图标栏隐藏；**贴左缘的按钮**点击召回。**阅读态的侧边栏是覆盖式**（`absolute` 浮在左侧桌边，**不占布局**）—— 用户定：*打开左侧菜单不应影响阅读器界面宽度*（占位式一展开会让纸右移、窄窗口下还会压窄纸、目录左缘也跟着漂）。⚠ **只给阅读态**：其他功能的交互形态需求未知，将来若第二个功能也需要，把条件扩成**显式能力标记**（如 descriptor 的 `floatSidebar`），不要散落判断 |
| **贴缘按钮形态** | 可见条**窄而长（6×160）**；按钮本体 **18×200**（比可见条**外扩一圈**）= 判定范围，方便鼠标弹出。被控对象展开（钉住）时按钮**跟着移到它的边缘**（侧边栏 → `left: var(--sidebar-w)`），永远贴边且可点 |
| **阅读参数面板（挂载线右段垂挂）** | 高频显示设计的入口（字号/行距/段距/纸宽/内边距）。**默认收起**（零控件不破例）：点挂载线右段或面板底部「折疊」（与目录同款）开合，**折叠 = 向上收回线里**；面板全透明、纯文字档位、发丝分割线、内容**中间对齐**、**遮罩按距离**（与目录同款）。`Esc` **先收面板**、再退阅读器 |

**目录（挂载线 + 垂挂列表）与阅读参数（底部挂件）——挂载线实体（2026-09-12 用户定）**

挂载线保持**横向**，但**延伸整个页面宽度**（`ReaderRail` 实体：左段目录挂载区 / 中段纯线跨过
正文上方 / 右段参数挂载区）：目录自线下方垂挂（形态下表不变）、**阅读参数面板挂在线右段下方**——
开合 = 点挂载线右段或面板底部「折疊」（与目录同款：居中 + 引导分割线），**折叠 = 向上收回线里**
（内容统一中间对齐；原右侧独立召唤条废除）。**低透明度与目录同款** = 遮罩按距离：文字全强度，
每行盖一层桌色遮罩，近 → 揭开、远 → 静息（`--reader-veil-rest`，缺省随 `--toc-veil-rest`）。两个挂件互不干扰（左/右分区）又同处一条线；**目录折叠时，点线（左段）=
展开目录**（线的点击职能）。快捷键：参数 `p`、目录 `t`。

| 项 | 规则 |
|---|---|
| **挂载线** | 一条**细横线**，自目录段起**延伸整个页面宽度**，是目录（左）与阅读参数（右）共同的**挂载点**（不是按钮外观、不是装饰分割线）。左缘 = `--sidebar-w`；目录段宽 = 左侧留白可用宽度（上限 `--toc-line-w`，暂 220px），参数段宽 = 面板宽（上限 `--reader-panel-w`，暂 250px）。静息 34% 不透明度、hover 75%；**目录段可点** = 目录开合，**参数段可点** = 参数开合（折叠时的收回热区） |
| **左缘对齐** | 挂载线与条目容器的**左缘 = 左侧边栏展开时的右缘**（`--sidebar-w`，AppShell 用同一个 token）—— 既不在屏幕边缘显得拥挤，也不占用/遮挡贴缘召回按钮。**宽度 = 左侧留白可用宽度**（上限 `--toc-line-w`）→ 默认不侵入纸 |
| **从线上垂挂** | 展开后条目**自线下方开始、逐条向下**排列（一条条向下 ≠ 卡片/抽屉盒子），左端与线的左端对齐 |
| **容器全透明** | 条目容器**完全透明**（没有底色）。层级靠排版与留白，不靠底色（对应 §2 原则） |
| **高亮 = 遮罩按距离** | **不是 hover 改文字颜色**。条目文字给足强度（`--text`），明暗由**盖在条目容器上的一层桌色遮罩**承担：不透明度在 **`[0, 静息值]`** 之间随**条目到鼠标的距离**变化（正中 → 0 完全揭开；超过 110px → 静息 `--toc-veil-rest`），过渡 **200ms** → 鼠标移动是一段"揭开"的动画，而非开关式高亮。⚠ **不变量：远端条目保持静息，靠近只做"揭开"，绝不比静息更暗**（公式 `veil = 静息值 × (1 − 接近度)`） |
| **静息更淡** | 静息遮罩 `--toc-veil-rest: 0.74`；「折疊」按钮静息 50%、hover 100% |
| **结尾折叠按钮** | 列表末尾一个文字按钮（「折疊」），**与条目容器中间对齐**；与条目区之间有一条**引导分割线**（否则条目滚到边界像凭空出现）；收回到只剩挂载线 |
| **默认展示** | 有目录的书**默认就是展开的**；**点击条目跳转不收起**；只有用户主动「折疊」/`t` 才临时隐藏（不持久化，换书回到展示态） |
| **不遮正文的理想态** | 浮层不改变纸的位置与宽度（`position:absolute`，不参与布局）；纸够宽时目录落在左侧留白内 |

> 例外登记（§5.2 例外清单）：左右两条**贴缘召唤条**属浮层例外（可有半透明底）。
> **阅读页没有任何常驻栏** —— 进度线/顶栏这类"浮现栏"不算例外，不得再加。
> 无头自检硬契约：正文宿主保留 `id="page-area"`；侧边栏「阅读」按钮保留（`aria-label="阅读"`）供真实用户路径断言。

### 5.9 阅读页可调参数清单 + 预留（**登记，逐步实现；不要 one-shot**，2026-09-11 定）

**为什么要这张表**（用户 2026-09-11 定）：阅读器的"可改"和"常改"是两件事 ——
**常改的参数必须随手可及，不常改的留在设置里**；而"随手可及"不得退化成常驻控件面（§5.8：阅读页零控件）。
下表是判断依据，动手前先回这里看频率与改法。

**A. 参数清单（按"改动频率"排）**

| # | 参数 | 影响面 | 现状 | 改法（技术路径） | 频率 |
|---|---|---|---|---|---|
| 1 | **字号** `fontSize` | 正文 | **已落**（挂载线右段面板 15/17/19 + 默認） | **注入** `setStyle`（`applyTypography`）：`html,body{font-size}` —— 书用相对单位会随之缩放；缺省档 = 不注入 | ★★★ 高频 |
| 2 | **行距** `lineHeight` | 正文 | **已落**（挂载线右段面板 1.5 / 1.75 / 2 + 默認） | 注入（同上，打 `html,body` + 常见块级元素） | ★★★ 高频 |
| 3 | **纸宽 / 一行长度** `--read-width` | 宿主容器 → kookit 排版宽度 | **已落**（620/760/920，px 落库 `readerSettings.readerWidth`） | 设置写 `documentElement` 的 `--read-width`（宿主 `clientWidth` 即排版宽度依据） | ★★★ 高频 |
| 4 | **段距 / 首行缩进** | 正文 | **段距已落**（0/8/16 + 默認，打 `p` 下边距）；**首行缩进未落** | 注入（`p` 的 `margin` / `text-indent`） | ★★ 中 |
| 5 | **字距** `letter-spacing` | 正文 | 未实现 | 注入 | ★★ 中 |
| 6 | **纸内边距（正文到纸边的距离）** | 正文 | **已落**（`--page-pad-x`，档位 24/44/72） | 适配器注入 `body{padding-inline}` —— 这就是"出血"的可调旋钮。⚠ **不能给宿主容器加 padding**：kookit 的排版宽度读宿主 `clientWidth`（含 padding）会对不上；注入 body 是安全的（`handleImageSize.getContentWidth` 会扣掉父容器 padding，图片上限自动收进来） | ★★ 中 |
| 7 | **字体**（衬线/黑体/自备） | 正文 | 未实现 | 注入 `font-family` + 打包字体（源流明體已有；**引新字体先核许可证**） | ★★ 中 |
| 8 | **主题（取向 × 模式）** | 全局 | **已落** | `data-theme` + `IRenderService.applyTheme` | ★★ 中（场景性） |
| 9 | **布局模式**（滚动/单页/双页） | 渲染 | **已落** | kookit config（**重开书生效**） | ★ 低 |
| 10 | **对齐**（两端对齐等） | 正文 | 未实现 | 注入 | ★ 低 |
| 11 | **中文标点/混排开关** | 正文 | §4 已是硬规则 | 注入 `text-spacing-trim` / `text-autospace`（Chromium 130 支持度需实测） | ★ 低 |
| 12 | 每行列数（measure） | — | 与 #3 同源 | 改纸宽即改一行长度，**不单列参数** | — |

> 技术前提（2026-09-11 确认）：`rendition.setStyle(css)` 是正文 iframe **唯一的样式注入口**
> （`KOOKIT.md` §5；本 bundle 的 `StyleHelper.getDefaultCss` 从未被调用），深色注入已走通这条路 ——
> 所以 #1/#2/#4/#5/#6/#7/#10/#11 全部**可行**，不是"方案未定"。
> 注入要合并成**一份** reader style（深色 + 排版参数），避免多次 `setStyle` 互相覆盖。

**B. 预留（不实现，先登记）**

| 预留项 | 口径 |
|---|---|
| **字距 / 字体 / 对齐 / 首行缩进 / 中文标点开关** | 表 A 的 4/5/7/10/11 项，改法都是注入，**改法已定、未实现**；实现时按"频率"决定进挂载线面板还是设置页 |
| **自由调节 / 自适应** | 远期：滑杆（连续值）+ 按屏幕尺寸/字号自适应纸宽。数据层已是 px 数值，**换 UI 不用改模型** |
| **右侧"模型阅读"容器** | ⚠ 右侧现在已经有阅读参数面板 —— 将来"模型阅读"若也要右侧，需先定**共存形态**（同侧切换？上下分区？还是独立第三区）。原则不变：**只占留白，不挤压纸的语义** |
| **同步相关控件** | 本产品是同步阅读器，阅读页还需要同步相关控件；**待房间同步打通后再定形态** |

排期见 `TODO.md`。

### 5.6 深色主题专项（**专项，部分未完成**）

用户 2026-09-09 指出：**暗色下问题很多**。这是**专项**，不在普通样式迭代里顺手改。

| 项 | 状态 |
|---|---|
| 视图切换动画在暗色下不可见（`--flash-bg` 与 `--bg` 几乎同色） | **已修**：`--flash-bg/--flash-text` 改为反色对（`dark` / `sepia-dark` 两套） |
| 「阅读器内部分文字被吞没」 | **已销案**：不是深色注入吞正文 —— 隔离测试（同书同冷态、浅/深背靠背）两侧 `bodyHtml` 与正文 `textContent` **完全一致**；症状来自**指标错**（拿 `innerText` 当"正文长度"，它是排版相关量，同模式两次运行实测 70/109 漂移）。非 PDF 深色注入本身有效，见 §5.8 |
| 其余动效与状态色的暗色表现 | **待复核** |

**剩余做法**：① 全 token 对比度审计（四套取向 × 每个文字色，含 muted/状态色/负片）；
② 阅读页实测四套取向（EPUB + PDF 各一本）；③ 结论回写本节并把规则补进 §3.4。
登记：`TODO.md`「渲染层风格与验收」组。

## 6. 禁区（机器可断言）

1. 组件内写死颜色（`#hex` / `rgb(` / `rgba(`）——只允许出现在 `styles.css`。
2. 白名单外的字号（§3.2）。
3. 衬线体使用非 700 字重（唯一豁免见 §5.4）。
4. 引入图标字体/图标库/彩色 emoji。
5. 多色强调、渐变、阴影（除抽屉）。
6. 组件内自行设置 `letter-spacing` / `text-indent` / `line-break`（归 §4 集中管理）。

## 7. 激进档与回退机制

**当前取向：激进文字化**（用户 2026-09-09 决定）——"大幅度使用文字取代，激进尝试以查看效果"。
为保证**时刻可回退**，遵守三条纪律：

1. **提交粒度 = 回退单元**：一次交付一个 commit，提交信息前缀 `style(client):`。
   回退单次：`git revert <sha>`；回退到某个时间点：`git checkout <sha> -- client/src/renderer`。
2. **逐组件推进**，不做批量全局替换（避免一次改坏全部界面且难以定位）。
3. **不删除旧语汇的代码路径**：被替换的样式若仍可能复用（如边框按钮），
   以注释形式保留在文件顶部（`// 旧档（framed）：...`），便于快速回填。

> 版本号是否滚动由用户决定（`docs/STATUS.md` §2）；本档改动并入当前版本，不自行发版。

## 8. 验收

### 8.0 效果确认（第一手段）

`cd client && npm run style` —— **样式样张**（`tools/style-gallery/`）：浏览器里看 token / 字号 /
中文排版 / 各组件状态，**不启动 Electron**。样张只导入真实组件与真实 `styles.css`，
因此"样张所见 = 组件实现"，不会漂移。改 token → HMR 秒级刷新。
（Electron 内的最终复核仍走 `dev/selfCheck.ts`；样张不覆盖 IPC/窗口/kookit 渲染。）

### 8.1 机器断言（在 `dev/selfCheck.ts`）

**已实现**（跑一次无头自检即覆盖）：

- `字体=ok` —— 打包衬线体可用：`document.fonts.check('700 16px "GenRyuMin TW"')`。
- `文字封面=ok` —— `FittedTitle` 撑满（字号与填充率双向判据）。
- `夜間注入=ok` —— 正文样式注入前后**正文不许变**（`textContent` 与元素数相同）且注入确实落地；
  **PDF 跳过**（`applyTheme` 对位图本就是 no-op）。
- `注入樣式=` 事实行 —— 注入字符数 + body 的**计算样式**（字号/行距/内边距），证明参数真的进了 iframe。
- `滚动条=隐藏/显示(溢出NNN)` —— 是否真有内容溢出（扣除 kookit 的 +300px 余量）。
- `恢复=ok` —— 走真实用户路径（点侧边栏「阅读」）后**正文确实回来**；判据按格式分（PDF 数子 iframe 的 canvas）。
- 渲染主判据：`可滚动 > 0` 且翻页生效，内容量一律用 `textContent`（**不用 `innerText`**，见 §5.6）。

**未实现**（待办见 `TODO.md`）：

- **无边框文字按钮存在性**：书库视图切换 / 導入按钮的 `borderColor` 为 `transparent`。
- **字号白名单扫描**：遍历渲染树，`fontSize` 必须落在 §3.2 阶梯内（封面自适应除外）。

### 8.2 grep 断言（CI/审查用；**尚未接入，待办见 `TODO.md`**）

- `#hex` 只出现在 `styles.css`；
- `font-family` 只出现在 `styles.css`；
- 组件里不出现 `letter-spacing` / `text-indent`。

### 8.3 新组件检查表（5 条）

1. 这个入口/动作**必须**是文字吗？能不能去掉边框？（§5.1）
2. 文字属于哪个声部？字重是否合规？（§3.1）
3. 颜色是否全部走 token？是否只强调了一次？（§3.4）
4. 层级是否靠字号/留白而非颜色？（P3）
5. 动效是否必要、是否 ≤ 650ms 且只有一个？（P5）

## 9. 参考谱系

**哲学 / 规则集**
- [iA Writer · From Monospace to Duospace](https://ia.net/topics/in-search-of-the-perfect-writing-font/) —— 一套字体解决专注。
- [Butterick's Practical Typography · Summary of key rules](https://practicaltypography.com/summary-of-key-rules.html) ／ [Typography in ten minutes](https://practicaltypography.com/public/typography-in-ten-minutes.html) ／ [Maxims of page layout](https://practicaltypography.com/public/maxims-of-page-layout.html)。
- [Swiss International Style 规则集](https://github.com/zeke/swiss-design-skill) ／ [Implementing Swiss Design Principles in Modern App UI](https://tunmie.bearblog.dev/implementing-swiss-design-principles-in-modern-app-ui/)。

**中文 / 日文排版**
- [赫蹏 heti](https://github.com/sivan/heti) —— 中文排版规范实现（标点挤压/悬挂、混排间距）。
- [W3C 日本語組版処理の要件（jlreq）](https://www.w3.org/TR/2012/NOTE-jlreq-20120403/ja/) ／ [W3C 纵排文本指南](https://www.w3.org/International/articles/vertical-text/index.en)。
- [Typotheque · CJK 組版](https://www.typotheque.com/articles/typesetting-cjk-text-jp)。
- [源流明體官方](https://buttaiwan.github.io/font/) ／ [genryu-font](https://github.com/ButTaiwan/genryu-font)（OFL 1.1，仅 Bold 切面）。
- [qiaomu-design · chinese-typography](https://github.com/joeseesun/qiaomu-design/blob/main/references/chinese-typography.md)。

**同类阅读器（对照）**
- [Anx Reader](https://anx.anxcye.com)（[评测](https://lemmy.ahhh251.xyz/c/android/p/77135/i-found-my-ideal-reading-app-anx-reader-review)）—— CJK 阅读器排版可调的成熟样本。
- [Legado 阅读 · 内容排版优化方案](https://blog.gitcode.com/a9303b682da15bfc8eb65e9f29705349.html) ／ [主题美化的三条路子](https://post.smzdm.com/p/a5rdz383/)—— 中文读者对字距/行距/段距的真实诉求。
- [Thorium Web · Reader Styling](https://deepwiki.com/edrlab/thorium-web/7.1-reader-styling)—— 界面样式与正文样式分层的工程做法。
- [Readwise Reader 的品牌美学](https://blakecrosley.com/zh-Hans/guides/design/readwise-reader)。

## 10. 修订记录

| 版本 | 日期 | 变更 |
|---|---|---|
| v0.9 | 2026-09-11 | **文档收束 + 发行版**：① §5.8/§5.9/§6~§8 去掉版本考古层、改成规范文本（历史留在本节）；修正**与实现不符**处（滚动条 2px；"短章节整条隐藏"的机制 = 适配器设内联 `scrollbar-width: none`，早前记的 `--stage-overflow` 变量已移除）② §5.6 深色专项：「阅读器内部分文字被吞没」**正式销案**（是"拿 `innerText` 当正文长度"的指标错，非深色缺陷），剩余 = 全 token 对比度审计 ③ §8.1 区分**已实现**与**未实现**的机器断言 ④ 新增发行版：`client/electron-builder.yml` + `npm run dist`，**只出 64 位 Windows**（NSIS 安装包；electron-builder = MIT，许可已核）|
| v0.8 | 2026-09-11 | **右侧阅读参数面板落地（§5.8/§5.9）**：① 高频显示设计（**字号 / 行距 / 段距 / 纸宽 / 内边距**）改为**阅读页右侧可召唤面板**（默认收起，贴右缘窄条与左侧召回条同款镜像；`p` 开合，`Esc` 先收面板）——"阅读页零控件"不破例 ② 新增注入端口语义：**宿主几何（纸宽/内边距）走 CSS 变量，正文排版（字号/行距/段距）走 `applyTypography` 注入**；缺省档 = 不注入（尊重书自带排版）③ 设置页只留低频项（布局模式），阅读宽度等**从设置页移出**（避免两个写者） |
| v0.7 | 2026-09-11 | **用户逐条纠正（§5.8）**：① 滚动条 **2px** ② **阅读态侧边栏改覆盖式** —— 打开左侧菜单**不再影响阅读器界面宽度**（占位式会让纸右移/压窄、目录左缘漂）；**只给阅读态**，其他功能态交互形态需求未知故保持占位（将来扩成显式能力标记）③ 贴缘按钮**更窄（6px）更长（160px）**，按钮本体 18×200 外扩一圈做判定范围；侧边栏展开时按钮移到其右缘 ④ **目录遮罩语义修正**：不透明度只在 `[0, 静息值]` 之间 —— 靠近只"揭开"，**远端保持静息、绝不更暗**（v0.5 的实现把远端加深了）|
| v0.6 | 2026-09-11 | **滚动条真相修正 + 目录细节 + 参数清单（用户逐条纠正）**：① **滚动条之前根本没变细** —— Chromium 121+ 支持 `scrollbar-width`/`scrollbar-color` 后，**一旦写了这两个标准属性，`::-webkit-scrollbar` 系列会被整个忽略**（Electron 33 = Chromium 130）→ 实测仍是 `thin` 的 ~11px。现改为**只用 webkit 伪元素**（当时 4px，v0.7 收成 2px）；短章节的判据交给适配器（溢出 ≤ kookit 的 +300px 余量就整条隐藏）。⚠ 当时用的 `--stage-overflow` 变量**已废弃**，现行机制 = 内联 `scrollbar-width: none`（见 v0.9） ② 挂载线点击判定区放大到 ~34px 高（线本身仍是 1px，视觉位置不变）③ 条目区与「折疊」之间加**引导分割线**（否则条目滚到边界像凭空出现）④ 新增 §5.9 **可调参数清单表**（12 项 + 频率 + 改法；确认 `setStyle` 注入可行，字号/行距等不再"方案未定"）⑤ **纸内边距落地**（`--page-pad-x` = 44px，适配器注入 `body{padding-inline}`）—— 正文不再贴着纸边；`applyTheme` 语义随之澄清（排版参数始终注入、颜色仅深色注入，`CONTRACTS.md` v0.3.2）|
| v0.5 | 2026-09-11 | **阅读器沉浸态三次修订（§5.8/§3.4，用户逐条纠正）**：① **纸与桌靠颜色区分** —— 新增 `--desk-bg`（纸外背景，四套主题各一组）+ 纸的 **1px `--page-edge` 边**；全屏的从此是"桌"、正文是"纸" ② **滚动条退场** —— 4px，静息 `--page-text` 10%（几乎不可见），指针进入正文列才 26% ③ **目录左缘对齐侧边栏展开时的右缘**（`--sidebar-w`，AppShell 共用同一 token）→ 不再贴屏幕边缘、不遮挡贴缘召回按钮；宽度受左留白夹取；**折叠按钮与容器中间对齐** ④ **高亮改机制** —— 不是 hover 改文字色，而是条目容器上盖一层**桌色遮罩**，不透明度随**条目到鼠标的距离**变化（110px 衰减）+ 200ms 过渡；静息遮罩 `--toc-veil-rest: 0.74`（比 v0.4 更淡）⑤ **目录默认展示**；点击条目**不**收起（只有「折疊」/`t` 临时隐藏，不持久化） |
| v0.4 | 2026-09-11 | **阅读器沉浸态二次修订（§5.8，用户纠正上一版的错解）**：① **全屏的是"纸"不是"正文"** —— 正文改为**居中定宽列** `--read-width`（620/760/920，默认 760；列宽 = kookit 排版宽度依据，宿主容器 `clientWidth`）；外层全屏纸 + 滚轮从留白转发给列 ② **阅读页上不再有任何栏**：删掉"临时顶栏（书名/页码/翻页/目录/关闭）"与"常驻细进度线"——**浮动出来的栏同样是状态栏**；退出 = `Esc`，鼠标路径 = 贴缘按钮召回侧边栏 ③ **目录 = 挂载线 + 垂挂列表**：细横线定名"挂载线"，贴近内容区左缘、默认不侵入正文列（长度被左留白夹住，`--toc-line-w` 暂 220px），条目自线下方向下逐条排列，**容器全透明**（原半透明底删除）④ 新增 §5.9 **阅读页预留（登记不实现）**：显示设计控件 / 右侧"模型阅读"容器 / 同步控件 |
| v0.3 | 2026-09-11 | **主题模型规正（§3.4）**：2 主题 × 2 模式（纯色/羊皮纸 × 深/浅），`--page-bg/--page-text` 深色模式改用该主题深色纸（不再"暗色仍白纸"）；**阅读器沉浸态规则（§5.8）**：全出血去圆角边框 / 侧边栏退场 + 贴缘半透明按钮 / 无常驻顶栏（临时浮现）/ 细进度线 / 目录浮层抽屉（左上分割线 + 透明无边框半透明容器 + hover 变深 + 底部折叠）/ 键盘鼠标翻页 / Esc 退出 |
| v0.2 | 2026-09-09 | 字体由三声部**收敛为全局统一**（§3.1，Times New Roman + 源流明體）；负片用途扩为两处（抽屉 + 侧边栏选中，§5.5）；抽屉平面结构定稿（封面 2:3 + 三行三等分 + 单行滚动 + 无标签指标行，§5.5）；状态栏左对齐 / 无书目数 / 22px + 0.08em（§5.1）；设置页改分组结构（§5.7）；深色主题专项登记（§5.6）；验收断言标注"尚未实现" |
| v0.1 | 2026-09-09 | 首版：五条原则 / 字体声部 / 字号阶梯 / 中文排版 §4 / 例外清单 / 禁区与断言 / 激进档回退机制 / 参考谱系 |
