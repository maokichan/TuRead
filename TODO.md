# TuRead 待办（TODO）

> 全项目唯一待办清单：server / client / 跨端。完成一项即删除或移入 `docs/STATUS.md` 版本历史；
> 新增待办先登记这里。带 (P1/P2/P3) 的是债务优先级。

## client

> 分组口径（2026-09-09 整理）：按**改动落点**分域 —— 渲染/阅读器 ｜ 书库 UI ｜ 索引与容器（新需求组）｜
> 笔记与阅读行为数据 ｜ 同步（云端）｜ 标准化与身份 ｜ 风格与验收 ｜ 工程设施 ｜ 远期插件。
> 每组内按优先级排（高优/专项 > P1 > P2 > P3 > 未标）；新增待办先登记到对应分组。
> 原「从各文档誊挪过来的待办」一节已并入对应分组（溯源标注保留在条目标题内）。

### ★ 主线收口：「书的身份」已落地并发版 v0.1.17（2026-09-15）

> **讨论与实施都已结束**，逐项记录（四处自查修正、阻塞级 bug 的真因与修法、全部验证数字）
> 已移入 **`docs/STATUS.md` §4 的 v0.1.17 条目**；契约 = `client/docs/DATA_MODEL.md` §4.2/§6 + `CONTRACTS.md` v0.4.0。
> **本清单不再保留已完成的实施步骤**（TODO 只放待办）。
> ⚠ **唯一未做的是真机验收**：多库同一本书的笔记/进度是否真共享、映射库扫描手感、移除后笔记是否留存。

**以后再说（已登记，不阻塞实施）**：F3 孤儿 edition 的清理动作 · F5「全部书」顶层视角 ·
F6「导出库包」 · F10 Pro 功能边界（见 `DATA_MODEL.md` §6.4 与下方「产品与发行」组）
＋ **F11 同库多路径的收录键**（2026-09-15 实现期发现）：`holdings` 键是 `(library_id, edition_id)`，
所以**同一库内**同一份内容放在两个文件夹时只会有一条收录、`path` 指向**扫描最后遇到的那个**
（表现：书只出现在其中一个文件夹、换次扫描可能换位置）。候选：键放宽为
`(library_id, edition_id, path)`（允许多路径收录、UI 去重），或明确"一库一内容一次收录、
位置以首次为准"。详见 `DATA_MODEL.md` §6.4。
＋ **F12 迁移器两条未覆盖边界**（2026-09-15 迁移夹具的复核报告，**不是 bug，是口径待定**）：
① **`reading_state` 合并判据是 `lastReadAt ?? 0`**（`migrate.ts:252`）→ "只有位置没有时间"的那条
**永远压不过带时间戳的旧记录**；若新版位置更近却没时间戳，会保留旧位置。
候选：比较时把"无时间戳"当作**比任何时间戳都新**（位置是用户最后一次动作，时间戳只是辅助）。
② **单个旧库内若存在同指纹两行**（v2 `books` 表没有指纹唯一约束），`holdings` 的 `INSERT OR IGNORE`
（`migrate.ts:233`）会**静默丢掉第二条**（每库每 edition 只允许一条收录），其笔记会并到同一 edition
—— 设计上说得通，但**数据上不可见**（用户不会知道少了一条）。候选：迁移时把这种情形
记进日志/`meta`，让"悄悄丢"变成"有痕"。

### 渲染与阅读器（本地）

- [ ] **(用户复测 2026-09-13) 分页模式交互问题仍在**：用户报单页模式滚轮/目录不可用。已修两处
  真根因（iframe 桥嵌套盲区 → PDF 子页；左右点击翻页带滚轮死区）+ 无头探针
  `dev/pagedInteractProbe.ts` 实证 EPUB 单页键盘/滚轮/目录三路正常（合成事件）。**待复现细节**：
  书格式（EPUB/PDF）、鼠标位置、操作序列——EPUB 真实滚轮若仍异常，候选 = 原生横向滚动与
  next() 的竞态（探针派发的是合成事件，绕开了原生滚动路径，需要真实鼠标复现）。

> 已完成项（封面/元数据离屏解析、书库窗口化渲染、TXT 编码修复、MD 渲染验证等）不再占本清单——
> 内容已并入 `docs/STATUS.md` §4 版本历史（v0.1.13）与 `client/docs/FEATURES.md`，当作程序既有能力看待。

- [ ] **全局搜索接线（2026-09-12 用户定：标题栏搜索 = 全局，按功能域分作用域）**：
  书库作用域已实装（标题/路径过滤）；**阅读器作用域** = `IRenderService.search`（引擎内搜索，
  返回形状 CONTRACTS §7 待定——接线时一并定）+ 结果面板（跳转走定位系统）；**房间作用域** =
  搜房间列表 / 服务器书目（`GET /rooms`、远期 server 书目检索）。占位与作用域占位文案已在
  TitleBar（未接线的域标注"未接線"）。
  ⚠ **与「笔记管理」的检索不是一回事**：那条**只搜笔记**（`excerpt` 摘录 / `body` 批注），
  本条的阅读器作用域搜的是**书内全文**（引擎侧）—— 两条互不替代（见下「笔记与阅读行为数据」组）。
  ⚠ 另：**跨书库全文搜索**（所有书的全部文字）**没有待办**，属新专题（要抽全书文本，扫描版 PDF 无文本层）。
  ✅ **笔记作用域的入口形态已定（2026-09-16）**：搜索框在**标题栏**（与"按功能域分作用域"一致），
  实现 = `TitleBar` 扩为**按 `FeatureId` 分表的搜索词** + `app.focusSearch` 分流扩到「书库 / 笔记」
  —— 落在「笔记管理」专题的实施步骤里（本条目不再重复）。
- [ ] **自建目录（2026-09-12 用户立项，可行性已评估=现实；2026-09-15 用户定 → **Pro 候选**）**：① TXT 分章 kookit 内建中文规则
  （第X章/節/回/卷/篇…+CHAPTER/序章等，`parserRegex` 可自定义——可做成设置项）；② **PDF 页码目录**：
  数据现成（chapterDocIndex=页码），当前被"全数字=假目录"过滤整体不显示 → 给 PDF 做专门的页码跳转目录；
  ③ MD：marked 后自建 h1-h3 层级目录（kookit 只给 h1）；④ EPUB 无 toc 书的启发式兜底（远期）。
  依据：ReaderFeature 只在 `toc.length>0` 时渲染挂载线——"部分书目录打不开"的根因即此处为空。
  ⚠ **与「书的身份」实施的接口（2026-09-15 登记）**：**目录是 edition 级数据**（同一作品不同版目录不同，
  **不能挂 work**），且现状**完全不落库**（每次 `open` 由 kookit 现算）→ 功能虽归 Pro 候选，
  但 `edition_toc(edition_id, …)` 的**存储位要在「书的身份」这次建 schema 时预留**
  （见文首 ★ 块第 ⑧ 步），并先定"与 kookit 自带目录如何合并"。（功能归 Pro 候选，见「产品与发行」组。）
- [ ] **(P2) 打开书的解析/渲染在主窗口主线程**（2026-09-12 摸底，与封面离屏同域的剩余项）：
  `open()`（读文件 IPC + kookit parse + renderTo 首章）与翻页懒渲染都在主线程——目录也在 parse 时
  一次性构建。跨进程挪不动（iframe DOM 必须在可见窗口），缓解方向：打开 loading 态、进书库后后台
  预热上次在读的书、章节级首屏优先。大书打开卡顿明显时再做。
- [ ] **(用户登记，明确"不要 one-shot") 阅读参数的剩余部分**（依据 `client/docs/STYLE.md` §5.9 的频率表）：
  - **已落（2026-09-11 v0.8；2026-09-13 扩）**：**字号 / 行距 / 段距 / 纸宽 / 纸内边距 / 布局模式** ——
    入口 = 挂载线右段**阅读参数面板**（**默认展开**，2026-09-13 用户定；布局模式从设置页移入，
    改 = 重开书生效）；数据层存 px 数值，档位只是呈现。
  - **未落（改法与可行性都已定 = `setStyle` 注入；按频率决定进右侧面板还是设置页）**：
    **字距**、**字体**（打包字体，引新先核许可证）、**对齐**、**首行缩进**、**中文标点/混排开关**。
  - **远期**：滑杆自由调节 + 按屏幕尺寸/字号自适应（数据层已是数值，换 UI 不改模型）。
  - **右侧"模型阅读"容器（预留）**：⚠ 右侧现在已经有阅读参数面板 → 将来模型阅读若也要右侧，
    **先定共存形态**（同侧切换 / 上下分区 / 独立第三区）；原则不变：只占留白，不挤压纸的语义。
  - **同步相关控件**：本产品是同步阅读器，阅读页还需要同步相关控件；**待房间同步打通后再定形态**。
- [ ] **(2026-09-13 用户登记) Veil（遮罩按距离）优化**：现实现 = 逐条目测量矩形 + 逐条目写
  CSS 变量（`TocPanel.paintVeil` / `ReaderControls.paintVeil`，每帧 mousemove 重画）。用户提出：
  未来未必要这种逐元素/逐像素思路，可用更智能的方法（如纯 CSS 距离驱动、合成器层遮罩、
  预计算查找表）。当前条目规模下无性能瓶颈，**优化前先量化**；同批复核遮罩在 60fps 滚动/大列表下的表现。
- [ ] **(2026-09-12 用户登记) 阅读器新手交互提示**：进阅读器默认隐藏左侧边栏，用户不知道怎么呼出——
  在**左下角用低不透明度小字**提示可呼出（如「側邊欄　貼緣召喚 / Esc 離開」），首次若干秒后淡出
  （对齐「注意力焦点」判断：淡出要慢、召回恒定，见 `docs/STATUS.md` §3 判断③）。
- [ ] **(2026-09-13 用户立项) 模糊（blur）动效**：把页面模糊当作**注意力工具**——
  ① **大状态切换的过渡模糊**：切换布局模式（原地重开书）等会整页跳变的动作，切换瞬间对纸容器
  加短促模糊过渡，降低跳变感；② **定向模糊 / 聚焦**：引导读者专注于某一部分（如挂载线面板、
  目录展开时正文轻微模糊，收起即还原）。
  - 实现：宿主纸容器 CSS `filter: blur()`（合成器层，代价可控）+ 过渡动画；正文本体（iframe 内）不动。
  - ⚠ 性能边界：含 iframe 的整页模糊在**连续滚动/大位图（PDF）期间**的代价值得实测——
    先只做**状态切换瞬间**的短模糊，滚动路径不加。
  - **形态待定**（动手前先定）：哪些动作算"大状态"（布局模式/主题切换/进出阅读器）；
    定向模糊是切换瞬间效果还是常驻"聚焦模式"（对齐注意力焦点哲学：淡出要慢、召回恒定，见 `docs/STATUS.md` §3 判断③）。
- [ ] **(P2 · 安全/契约，2026-09-15 行为审查登记) PDF 的正文 iframe 不带 sandbox（文字类带）**：
  kookit `GeneralRender.ts:159` 是 `isAllowScript = format === "PDF" || isMobile === "yes" ? "yes"
  : config.isAllowScript || "no"`，而 `layoutUtil.ts:266-268` **只在 `isAllowScript !== "yes"` 时**
  设 `sandbox="allow-same-origin"`。后果：**文字类**（EPUB/MOBI/AZW3/TXT/MD/HTML/FB2/DOCX）书内脚本
  被 sandbox 挡住（无 `allow-scripts`）→ "恶意电子书经 `window.turead.invoke` 读本地文件"这条链路
  **在文字类上不成立**；**PDF 则完全无 sandbox 属性**（kookit 注释称 PDF 强开 sandbox 会渲染不出，
  即 pdf.js 文本层/注释层依赖同源脚本 → **不能简单补 sandbox**）。
  `KOOKIT.md` §5.4 只记了"非 PDF 非移动才沙箱"这个事实，**未标出它的安全含义**。
  处置：① 先把这条列进 `KOOKIT.md` §5.4 的坑位说明（升级敏感区，属逆向事实）；
  ② 评估 PDF 侧可行的收紧路径（`webPreferences`/CSP/隐藏窗口隔离），不能靠"上游已挡"兜底
  （桥本身的收敛见「工程与测试设施」组的 IPC 桥条目）。

### PDF 专区（2026-09-13 开设：PDF 是位图载体，凡"重排/缩放/旋转/反相"都绕不开 kookit PdfRender 的固定像素渲染，集中登记）

> 已解决：PDF 改纸宽 = 填充（2026-09-13，原地重开方案，见 `docs/STATUS.md` 决策）；
> 量化存档在探针 `dev/pdfWidthProbe.ts`。PDF 专区其余条目见下。

- [ ] **(2026-09-13 用户提示) PDF 填充策略因情况而异**：纸宽填充已用"原地重开"统一落地，
  但不同 PDF（扫描版/文字版/加密本）与不同参数（字号/边距联动）下填充行为可能需要差异化策略
  ——随用户实测逐案优化，不追求一次到位。
- [ ] **(2026-09-13 用户立项) PDF 页面旋转**：扫描版 PDF 常有整本/成段页面被旋转（横排内容竖放），
  阅读器应提供旋转能力（±90°）。技术现实与修法候选：
  - kookit `PdfRender` canvas 固定像素、无公开重排入口（与上条同根）；pdf.js 页面渲染支持 rotate
    但 kookit 未暴露该参数（改 vendor 内部需走 KOOKIT.md 升级纪律）。
  - 候选：① 适配器注入 CSS `transform: rotate(90deg)` + 宽高互换（轻；滚动方向/页宽计算需适配）；
    ② 走 kookit 内部 `handleRenderPDFChapter` 渲染参数（依赖内部细节）；③ PDF 整本重开带 rotate 配置。
  - **形态待定**（动手前先定）：全局开关 vs 单书记忆（存 `BookRecord`/阅读状态）；左旋/右旋都给还是只给一个；
    进参数面板（仅 PDF 时显示该行）还是键盘快捷键（走意图层登记）。
- [ ] **PDF 分页模式（single/double）行为验证**：布局模式已进参数栏（2026-09-13），但 PDF 下
  分页模式是否生效、与纸宽问题是否叠加，均未实测（原纸宽条目注记抽出单列）。
- [ ] **PDF 夜间模式（像素处理）**：PDF 页面是位图，改文字颜色无效 → 对页面 canvas 做
  `getImageData` → 反相/降亮度 → `putImageData`（或容器 `filter: invert` + 色相补偿）；
  扫描版同理。方案已记录在 `client/docs/KOOKIT.md` §7，正式实现单独立项。
- [ ] **PDF 可见窗口交互抽查**：无头自检已含翻页断言（2026-09-07 全绿），缺真人窗口下的翻页/进度抽查。
- [ ] **(P3) harness 侧 PDF canvas 渲染待定位**：`renderPdfPage → handleRenderPDFChapter →
  section.load()/render()` 链路断点；自检断言需按 PDF 语义特判（查 canvas 数 + 页位置）——
  App 侧已不受影响，优先级低。
- [ ] **(关联) PDF 页码目录**：在「自建目录」项下（渲染与阅读器组）——PDF 的 `chapterDocIndex`=页码，
  数据现成，被"全数字=假目录"过滤整体不显示。

### 书库 UI 与交互

- [ ] **(高优) 键鼠操作 / 意图层**（2026-09-09 用户定；**骨架已落地 2026-09-12**：
  `core/domain/input.ts` 纯函数绑定表（normalizeKey/resolveIntent/assertNoConflict 一键一意）+
  `DEFAULT_BINDINGS`（现状键位原样收拢：reader.back/nextPage/prevPage/spacePage/toggleToc/toggleControls、
  app.focusSearch）+ `useKeyIntents`（ref 装载；常驻挂载组件必须带 enabled 守卫）。
  Reader/TitleBar 已迁移。**剩余**：① 书库域意图收编（行内键 Enter/Space/Delete 仍元素级；
  单击详情/双击打开/方向键移焦点）② 色块视图/房间域意图 ③ 用户自定义绑定表（表已是纯数据）
  ④ 新键（沉浸全屏 F9 候选等）一律走意图层登记。范围顺序：书库域先行 → 阅读器 → 房间
- [ ] **详情抽屉「描述」块的内容待定**（2026-09-09 用户定）：抽屉底部保留一个满宽负片块占位
  （**无标签**，当前显示「（描述待定）」—— 内容自述性质，见 `STYLE.md` §5.5）。
  显示什么未决定，候选：① `BookMetadata.description`（kookit 解析自带，但 `extractMetadata`
  目前只取文件名 → 恒空）② 用户手填的书评/备注 ③ 标准化后由 ISBN 拉取的作品简介。
  定之前保持占位，改动落在 `BookDetailPanel.tsx` + `CONTRACTS.md` §2 BookMetadata。
- [ ] **详情抽屉操作清单**：当前只有「打开 + 删除」（书库重做待定项，FEATURES §10）
- [ ] **网格卡片标题截断规则**：当前 `line-clamp-2`，是否改按行数/字数（原 FEATURES §10 待定 2）
- [ ] **抽屉与网格的遮挡处理**：抽屉已改**全透明**，"网格模式下被盖住的卡片如何避让"
  需重新评估（原 FEATURES §10 待定 5 的前提已变）
- [ ] **封面维护动作**：重建 / 清理 / 手工指定（原 FEATURES §10 待定 3）
- [ ] **文字封面的艺术化（后话）**：无封面时"文字封面"本身就是标题，列表行右侧还会再显示一遍标题
  → 同一串字出现两次。除"是否隐藏右侧标题"外，更想做的是让封面文字具备**艺术效果**
  （字号/字距/行距构成感、可能的竖排或字面造型），让它像一张封面而不是放大的标题。
  与 `FittedTitle` 同一处演进（`client/docs/FEATURES.md` §10 待定 7）

- [ ] **(P2 · 规模 · 2026-09-15 行为审查登记) 层级浏览的取数与封面取字节都不随规模收敛**：
  ① `SqliteStore.listBooksAtLevel`（`sqliteStore.ts:404-411`）的**虚拟映射分支**是
  `SELECT * FROM books` **全量拉回再在 JS 里按 `dirname(filePath)` 过滤**（另两个分支才是 SQL 过滤）
  → 每次导航 O(全库)；② `LibraryFeature.loadLevel`（`index.tsx:201-205`）对本层**每本书并发**
  `getCoverUrl`（IPC + readFile）→ 首次进入某层是一次**无上限扇出**（`coverCache` 有 in-flight 去重与
  URL 复用，故痛在首刷）；③ 附带复核：该分支 `dirname` 的规范化（去尾分隔符后比较）**跨平台一致性**
  值得单独确认（Windows 反斜杠 / 大小写敏感性）。
  处置：虚拟映射改 SQL 侧过滤（或落 `parent_folder` 列 + 索引）；封面取字节改限并发。
- [ ] **(P3 · 2026-09-15 行为审查登记) `coverCache` 跨库切换不清理对象 URL**：
  `features/coverCache.ts` 的 `urls` 只在**书被移除**时 `revokeObjectURL`（`forgetCover`）；
  切库不复位 → 旧库的对象 URL 累积到应用退出（bookId 是 uuid，不会串书，纯内存累积）。
  处置：切库广播（main 的 `store:library-changed`）时清空缓存。

### 索引与书库容器（新需求，2026-09-09 定界 —— 区别于纯显示层）

- [ ] **多书架 / 书库容器（考虑中，2026-09-09 群聊重新定界；2026-09-12 用户确认照做并给出双索引形态）**：
  容器/书架照常演进（用户总要用一种方式索引信息）；「不管理源文件」判断（`docs/STATUS.md` §3）只约束**源文件层面**——
  不管理源文件实体（现状已如此：导入=建索引不拷贝、移除=只删索引），不延伸到索引组织层。
  **双索引形态（2026-09-12 用户定）**：
  ① **真实路径索引**：一个总容器承载用户配置的所有文件夹路径（= 现有"导入来源"的显式化）；
  ② **虚拟映射索引**：用户自建不同层级的承载容器——即便真实文件夹内容完全扁平，
  也能通过虚拟映射组织出树结构方便索引（容器 = 引用不是拷贝）。
  当前只有单一书库 + 列表/网格两种排列，
  但**无索引能力**，书籍到百本后管理变复杂。引入中图管理模式的**前提是先定义"容器"实体**：
  - **容器 = 书单/书库（Collection）**：内含对若干书籍的**索引**（引用，非文件/副本）；**可递归包含子书单/书库**
    → 树状索引，通过索引访问书籍；可选**简易 tag 系统**。
  - **分类法 = 可自定义的规则**（中图/自定义，如用户自己的 `APH/ART/CFG/NOPTUV/Z` 体系，参考 `D:\Database\图书馆`，
    其中有独立的「书单」容器与分类树平级）——分类告诉书**怎么放**，**不定义放书的容器**；容器是实现的必然。
  - ⚠ 索引容器会与本地文件夹**冲突**（书是引用不是拷贝），需解决；影响 `ILibraryStore` 与 `BookRecord`
    （bookId → collectionId 归属）、导入目标、视图状态与房间标定选书流程 —— 动手前先定模型
    （原 FEATURES §10 待定 6），见 `client/docs/FEATURES.md` §10 待定项
- [ ] **第三种视图：色块（封面抽象色块）**（2026-09-09 用户定，区别于列表/网格的新显示模式）：
  百本规模封面视觉噪音大 → 封面抽象为**纯色块**（主题色），悬停看原图，可开关。
  - 色块来源：封面管线已有 400px canvas 缩略图 → downsample 取主色，落 `BookRecord.colorSample`（近零成本）
  - **hover 看原图（方案已定）**：hover 时色块被真实封面缩略图 **crossfade 覆盖**，离开渐隐还原；
    不用 tooltip 浮层（会溢出压到邻居格子，破坏"整本书是块"的降噪语义）
  - 视图切换按钮扩为**三态循环**（列表→網格→色塊），显示当前视图名；`LibraryView` 扩 `'block'`
  - 键盘与另两视图统一（见「键鼠操作」高优项）：Enter 打开 / Space 详情 / Delete 移除 / 方向键移焦点
  - ⚠ 待定：四套主题「暗/亮黑灰白不引入色相」基线与**彩色色块**的冲突如何裁决（灰阶化 vs 保留书主色）
  - 与「文字封面艺术化」「网格标题截断」同属书库显示层演进（FEATURES §10）

### 笔记与阅读行为数据

- [x] ~~数据持久化 / 笔记划线落地（前主目标）~~ **已完成**（v0.1.16）—— 记录见 `docs/STATUS.md` §4。
  **遗留拆成下列独立条目**（本方向不再有"主目标"性质的待办）。
- [ ] **(笔记落地后的收尾) ① 真机验收**：真实鼠标走一遍 —— 右键菜单观感 / 批注输入手感 / 侧键翻页 /
  重开书高亮回显。**探针证不了手感与观感**（这正是用户批评过的"只证链路不证好用"，别再重复）。
  **② 笔记四色取值待用户过目**：`STYLE.md` §3.4（v1.2）已立案，**值可调**；口径由 kookit 的
  `rgba(色,0.8)` 叠加倒推 —— 纯色两套深色为冷调压暗、羊皮纸两套为暖调压暗。
  **③ PDF 第二批**：页+坐标的另一套 Fragment（`getSelectionAnchor` 现对 PDF 返回 `null`）。
- [ ] **(用户 2026-09-14 定，**待放行**再动代码) 笔记/划线概念收敛（`kind` 去 `'note'`）**：
  决定已记档（`CONTRACTS.md` §2 `NoteKind` + `DATA_MODEL.md` §3.2.1）：**笔记与划线是同一个实体**
  —— 锚点必然是一段正文范围（用户的理由："某一个笔记还是要关乎于某个内容的"）+ 可选 `body`；
  **不做"纯批注"**（只有标记点、无底色的形态）"没有太多必要"。
  收敛三步：① `NoteKind` 去掉 `'note'`（现存行迁移为 `highlight`）；② 交互层去掉两个命令
  （「標記」/「加批註」统一为"**标记 + 可选正文**"）；③ 呈现只按**有无 `body`** 区分
  （有 → 带批注图标；kookit 亦据此渲染 `isNote` 与 hover tooltip）。
  ⚠ 背景：现状用 `kind` 记**创建来路**（先划线后补正文 = `highlight`、直接写批注 = `note`，
  同内容两个名字 → 标签取决于历史而非当前状态），这是用户指出"交互上不明晰"的直接来源。
- [ ] **(用户 2026-09-14 立项) 配键体系（功能已确定，待定的是键位）**：
  ① **设置页维护可配置快捷键**（用户明确要求）—— 机制已备：`DEFAULT_BINDINGS` 是**纯数据表**
  （`domain/input.ts`），用户自定义 = 换一张表；需要做的是「默认表 + 用户覆盖」的合并、
  持久化（settings 表）、设置页 UI（含冲突检测——`assertNoConflict` 已可复用）与"恢复默认"。
  ② 远期：研究主流键鼠交互模式，**主要优化配键**（功能确定）。
  ⚠ **优先级（用户 2026-09-14 定）**：**"键盘做笔记不是最急的功能"** —— 键盘做笔记的完整链条
  （键盘翻页 → 选中文字 → 集中焦点 → 写批注，手不离开键盘）**行为已全部就位**
  （4 个 `reader.*` 意图已注册、批注面板 Enter/Shift+Enter/Esc 已实现、挂载即聚焦），
  **只差填键位**；故本条整体靠后，不受阻于它。
- [ ] **(用户 2026-09-14 立项，语义已确认) 房间域的返回/前进（侧键与通用键）**：
  用户澄清了房间域的实际结构：**进大厅 → 看房间详细页（有谁在读、在读什么书，可能含好友功能）
  → 通过房间建立共读后，仍然回到阅读页面的事**。因此**房间域的「返回/前进」= 在「房间详细页 /
  好友详细页」之间切换**（与书库域的"层级后退/前进"同构，只是遍历的是详情页而非文件夹）。
  落地口径：复用书库域已有的机制（历史栈 + 标题栏按钮 + 鼠标侧键 `XButton1/2`，见 `libraryNavBus`
  先例），**按功能态分工**（书库=层级 / 阅读器=翻页 / 房间=详情页）；当前房间域尚未实现，随
  房间 UI 一起做。⚠ 房间与真实 server 打通本身还有三处协议漂移未修（`STATUS.md` §4），属前置。
- [ ] **(用户 2026-09-14 立项) 色彩方案分层：色序（主题数据）↔ 语义槽位（程序契约）**：
  用户定的两个概念 —— ① **程序只负责"调用色序里的某个色彩"**（语义槽位契约固定：
  `bg/panel/border/text/muted/accent/page-*/desk-*/note-*/ok|warn|err`…）；② **色序本身由各主题
  分别确认** —— 官方主题从**对比度与舒适度**出发，用户主题**允许"不符合舒适度但具特色"**。
  好处：造主题只需给一条色序，而非复制整套 token。
  用户已认可"**深色倒排 + 少量修正**"（纯反色会栽在对比度曲线：边框/悬停底/静音文字这些**中间档
  并不对称**；Radix/Material/Tailwind 都深浅各写一条 ramp 就是这个原因）。
  **落地要点**：① 单条色序**不能**覆盖语义色 —— `ok/warn/err/note-*` 必须是**别的色相**，否则
  "同色相表达不同状态"读者分不出来 → 结构 = **一条中性色序 + 一小族各自有色序的语义色**；
  ② `STYLE.md` §3.4 现写「新增取向 = 复制整套 token，不允许局部覆盖」，要改成**派生口径**（基线变更）；
  ③ 深色"必须低亮度"这条（kookit 把高亮包成 `rgba(色,0.8)` 叠在正文上）要**显式编码**，不能指望倒排自然给出；
  ④ `STYLE.md` §5.6 那条「全 token 对比度审计」TODO 正好当这套派生方案的**机器可测验收标准**。
  附带收益（用户原话"某些栏到时候可以填充一些图表"）：**色序 = 图表要的顺序色阶** ——
  现状是一堆孤立命名色，画不了图。
  技术前提已核：Electron 33 = Chromium 130，支持 `oklch()` / `color-mix()`，色序可**算出来**不必手写。

- [ ] **(切库遗留，低优) 各 Feature 的设置缓存不随切库热更新**：库级设置随库走（settings 表），
  但 ReaderFeature 的 `autoFullscreenRef` 等只在挂载/激活时读一次；切到主题不同的库后
  主题/阅读参数要到下次相关交互才生效。做法候选：切库广播后各 Feature 重读自己关心的键。
- [ ] **(用户定，下个版本) 迁移器退役**：迁移器是**临时工具**——只为"从 JSON 时代的旧安装升级"
  存在；引导文件确立后新安装不再产生大 JSON。待用户发版滚动一个版本后（旧用户都升过一遍），
  删除 `SqliteStore` 的迁移分支与 `LibraryEntry.legacy*Path` 字段（SQLite 探针/`rebuild:sqlite` 保留）。
- [ ] **(用户登记 2026-09-13) 跨库数据分歧** → **已并入文首 ★ 最高优先级块的第 ② 项「笔记身份」**
  （2026-09-14 用户定：与「可见性」「阅读时间」是同一个问题，合并讨论）。
  原登记的候选方向保留在那里：① 同指纹的书跨库共享状态/笔记（`books` 去重、`notes` 挂 fingerprint）；
  ② 库间"同步/导入状态"的显式动作；③ 单库全局化（书库只做组织视图，状态全局唯一）。
- [x] ~~书库双模式（虚拟映射 + 自建书箱）主体~~ **已完成**（建库二选一 / 资源管理器式层级浏览 /
  書箱一等条目 / 移动全量交互 / 全局禁选 / 层级后退前进 + 侧键）—— 记录见 `docs/STATUS.md` §4 v0.1.15。
- [ ] **书库双模式的剩余项**（原 v0.1.15 条目的「待落」，2026-09-15 整理时**保留下来**，别当已完成）：
  ① **書箱排序**（`container_books.sort` 已建模未接 UI；资源管理器语义 = 拖动重排或排序菜单，
  **动手前定形态**）；② **面包屑过长处理**；③ **映射库子文件夹的新书自动出现** ——
  这一项**已由「书的身份」的扫描对账实现**（扫描即把真实路径上的新书收进来），复核后可直接销案。
- [ ] **(书库管理完善，随书箱 UI 一起)**：~~重命名~~（**已落**：管理弹窗行内更名）~~新建命名~~（已落）；
  **移除引用**（协议已支持：`removeLibrary`，UI 未放——先定交互：弹窗直删 vs 确认流程）、
  **库目录可配置**（引导文件指向即读，封面随库目录 = 数据+外观一起携带）、窗口状态入引导文件。
- [ ] **跳转历史（状态机）**：阅读跳转（目录/注释/回跳）用**状态机**做前进/后退栈（undo/redo）——
  **行动树已驳回**（2026-09-09 群聊定案：体验归根结底是线性的）；随笔记落地后实施
- [ ] **(用户 2026-09-14 重申，优先级上调) 「阅读时间」统计 + 「总阅读时间」占位落地**：
  ✅ **前置已解除（2026-09-15「书的身份」定案）+ 存储半边已落**：
  - **模型已定**（`DATA_MODEL.md` §4.2 D4/D11）：**逐 edition 记录、按 work 汇总**
    （有 work 才跨版合并计时；无 work 退化为按文件）—— 即"会话时长挂在哪一层"这个原问题已答。
  - **存储已落**：`reading_state.total_read_ms`（累计投影）+ `reading_sessions`（原始会话）
    + `ILibraryStore.appendReadingSession`（**同一事务里累进 `total_read_ms`**）
    + `totalReadMsByWork(work)`；抽屉第三格**已接线显示**（`BookDetailPanel`：有值显示分钟，无值仍是「共 —」）。
  - ⏳ **仍缺（本次未做，需先定事件口径）**：**谁在什么时候记一次会话** ——
    进入/离开阅读器、切书、应用退出、后台/失焦是否暂停，以及最小计入时长（防"点开就关"刷出垃圾会话）。
    定了口径后，只需在 `ReaderFeature` 的生命周期里 `appendReadingSession`，**不必再改表**。
  - ⏳ 另缺：**按 work 汇总的展示入口**（"我在这本书上花了多少时间"跨越两个 edition 时）——
    现抽屉显示的是**当前 edition** 的累计；跨版汇总要有 UI 落点（与「笔记管理」同批做）。
  - ⚠ **呈现形态已定（用户 2026-09-15）：阅读时间有自己的弹窗与界面** → 本项按**专题**做，
    不与抽屉占位混在一起（专题内容：会话事件口径 → 计时 → 弹窗界面 → 按 work/库/时间维度汇总）。
- [ ] **【专题 · 用户 2026-09-15 定】笔记管理 → 左侧工具组件（跨书浏览与管理）**：
  ⚠ **入口形态已定**：不再考虑"书库侧栏第三个入口 / 书库页下的视图"，
  **笔记管理 = 左侧工具组件的一部分**（与書/閱/房/設 同一侧栏体系内的工具组件）。
  动机（用户原话）："当用户的笔记积累到近千条，管理和索引也就变得同样重要了"。
  现状：笔记只在**阅读器内的左挂件**（`NotesPanel`）按**当前书**列出 —— 换书就看不到别的书，
  且没有检索/排序/跳转他书的能力。需要：① 跨书列出全部笔记（**分组形态已定 = 平铺** + 三态筛选/颜色/关键词）
  ② 双击 → 打开对应书并跳到锚点（`resolveAnchor` + `revealNoteId` 已具备；通道见下）
  ③ 检索（原文摘录/批注正文，**标题栏·笔记作用域**）④ 批量操作（清理/导出？**仍未定**）。
  ⚠ **本项按专题做**（用户 2026-09-15）：不与其他书库 UI 改动混批。
  ✅ **契约与视觉都已立案（2026-09-16）**：权威 = `client/docs/FEATURES.md` **§12**（形态/交互）+
  **`STYLE.md` §5.10**（视觉语汇；连带修订 §5.1 三角负片与 §5.2 例外⑤）+
  `CONTRACTS.md` **v0.4.1**（§4.4 `listAllNotes`/`countAllNotes` + `NoteQuery`/`NoteListItem`）与
  **v0.4.2**（`NoteView`/`NoteFilter`/`NoteTextFocus`/`NoteSettings` + §4.6 `IClipboard`）+
  `DATA_MODEL.md` **§5 问题 6**（检索批复）。**裁决汇总**：
  ① 容器 = **同一套容器、同级功能组件**（「工具组件」**就是**既有「功能组件」，**不引入 `kind` 分类**），
     只新增一条 `FeatureDescriptor`（id `notes`，符号「筆」入正常流，`設` 仍钉底）；
  ② 作用域 = **当前库为默认、可切「全部庫」**（笔记挂 edition → "某库的笔记"是经 `holdings` 的**查询口径**）；
  ③ 检索 = **只搜笔记**（`excerpt` + `body`），入口 = **标题栏搜索栏·笔记作用域**；**v1 用 `LIKE` 子串**
     （2000 条中文笔记实测 **0.32 ms**）；**FTS5 不上**（换装条件与两个静默坑登记在 `DATA_MODEL.md` §5 问题 6）；
  ④ 章节标签 = 「**書名 · 節 N**」，**章标题不冗余存**；
  ⑤ 视图 = **網格 / 瀑布流（默认），无列表**；**列宽固定、只变高**（摘录 ≤4 行 / 批注 ≤6 行）；
  ⑥ 文字主次**两档可切换**（默认**批註為主**）；筛选 **单按钮三态循环** `批註→劃線→全部`（默认批註，
     **判据 = `body` 是否为空，不是 `kind`**）；作用域两态循环；
  ⑦ 交互 = **单击选中 / 双击跳转**，**卡片零 chrome**（无 hover 动作区）；**右键 = 挂载线菜单**
     （`跳轉` / `複製批註`（仅 body 非空）/ `刪除`）；**不放开拖选**，複製走 `IClipboard` 端口；
  ⑧ **用词纪律**：书库「移除」= 只删索引 vs 笔记「**刪除**」= **真实删除**（两词不混用；确认弹窗写明
     不可恢复、**不给"下次不再提示"**）。
  ⚠ **契约缺口已补**：跨书跳转原**无通道**（`FeatureHost` 只有 `openReader(bookId)`）→ 定为
  `openReader(editionId, target?: { revealNoteId? })` + `FeatureProps.readerTarget`（**带 tick 防重复消费**）；
  **ReaderFeature 消费时序**：书已打开 + 笔记载入 + 目标章 `rendered` **之后**再 reveal（顺序错会静默落空）。
  **剩余 = 实施**（按序）：
  ⏳ **进展（2026-09-16）**：**视觉层已落** —— 领域类型（`NoteView`/`NoteTextFocus`/`NoteFilter`/`NoteSettings`）、
  `styles.css` 的 `--note-col-w` 与 `.note-flow` / `.note-card*` 语汇、`components/NoteCard.tsx`（纯展示：三段卡片 +
  两档主次 + 标记块）、`components/NoteFlow.tsx`（**網格 / 瀑布流**两态；瀑布流 = `grid` + `grid-row-end: span N`，
  首帧按文本长度估算、`ResizeObserver` 实测纠正），并在**样式样张**加了可交互一节（切视图 / 切主次 / 点选中态）。
  **实测**（`tools/style-gallery/smoke.cjs`）：6 张卡片高度 **59 / 88 / 101 / 110 / 220 px 共 5 档** → 证"高度随内容变"；
  `pageErrors=[]`。（样张黑屏已另修，见「工程与测试设施」组。）
  ① `FEATURE_IDS` + registry 条目 + `NotesFeature` 骨架（**激活时重读**）；
  ② `SqliteStore.listAllNotes/countAllNotes`（`holdings` 连接 + `body` 判据筛选 + 子串 + 排序/分页）+ IPC + 适配器；
  ③ `IClipboard` 端口 + preload **具名方法**（不用泛化 `invoke`）+ `ServiceContainer` 装配；
  ④ `TitleBar` 搜索扩为**按 `FeatureId` 分表**（现只有书库一路，且 `key={activeFeature}` 会重挂输入框）
     + `app.focusSearch` 分流扩到「书库 / 笔记」；
  ⑤ 视图 UI 的**其余部分**（底部状态栏 5 件 + 空态 + 筛选/作用域接线；卡片与流已落）；
  ⑥ 跳转链路（`openReader` 扩参 + ReaderFeature 消费，含一次重试）+ 右键菜单（`ContextMenu` 复用）；
  ⑦ ⚠ **瀑布流 × `useVirtualRange` 的等高 `stride` 假设冲突**：先量化（千条实测），
     候选 = 列高前缀和虚拟化 / 不窗口化 / 只给网格窗口化（`STYLE.md` §5.10 末段）。
  ⚠ **倒查表结论（2026-09-14，见 `DATA_MODEL.md` §5 开放问题 6）与它照出的两个实体层问题已全部了结**：
  ① 笔记身份 → **v0.1.17 已定**（笔记挂 edition）；② 章标题 → **2026-09-16 批复 v1 不做**（列表用书名）。
  索引 v3 schema 已备（`idx_notes_updated` / `idx_notes_owner`），**无需新实体、无需新依赖**。
- [ ] **(P3) 2026-09-12 架构审查登记的下沉项**：① ReaderFeature 的"全数字 = 假目录"过滤是**领域策略**，
  现在 UI 里——随「自建目录」落地时移入 domain；② 阅读位置的 2s 节流/flush 持久化策略在 ReaderFeature——
  下沉为 usecase（如 LocationPersistence）；③ `DEFAULT_READER_PARAMS`（控件三态模型）与领域
  `ReaderSettings` 的 null/undefined 双形状——控件模型留 UI 可接受，换 UI 时收敛。
- [x] ~~`Note.notes` 与 kookit 不匹配~~ **已销案**（v0.1.16 随 Note v2）—— ⚠ **原诊断方向记反了**：kookit 要的就是字符串（详见 `CONTRACTS.md` §8 v0.3.9 与 §2 Note）。

- [ ] **(P2 · 2026-09-15 行为审查登记) 笔记写透无补偿（库与 UI 可能分叉）**：
  `ReaderFeature.createMark`（`index.tsx:297-325`）先 `store.addNote` 再 `render.createNote`；
  `removeNote`（:366-379）先 `store.removeNote` 再 `render.removeNote` —— 渲染侧抛错时**库已改、UI 未改**，
  三处 `catch` 只 `pushLog`。后果：幽灵笔记（下次开书回挂出现）／已删笔记仍显示到换章。
  ⚠ **契约缺口**：`CONTRACTS.md` §4.1（v0.3.9）只规定 `createNote` 载荷不就位时**抛错**（不静默 no-op），
  **没有规定"已经落库了怎么办"**。处置：先定补偿口径（回滚库 / 补渲染 / 落"待修"标记择机重试），
  再改代码 —— 别直接 try/catch 加宽了事。

### 同步（云端 · RoomFeature）

- [ ] **(P2) join 握手无超时**：`pendingJoin` 只被 room.join-ack resolve，断线/丢包时挂死；
  加 10s 超时走 `server-error`（`usecases/RoomSession.ts:101`）
- [ ] **(P2 · 2026-09-15 行为审查登记，与上条同处) `pendingJoin` 是单槽 + 节流发送无 rejection 处理**：
  ① `joinRoom`（`RoomSession.ts:112-117`）用 `this.pendingJoin = resolve` **覆盖式**赋值 → **两次并发
  join 会让第一次永久挂起**（上条只登记了"无超时"，没覆盖这个）；② `throttleSend`/其定时器
  （:308/:316）用 `void this.net.send(...)` → 断线后产生**未处理 rejection**（只落到
  `[dev] unhandledrejection` 控制台噪声）。处置：pendingJoin 改队列/带代次，或整体取消上一次 join。
- [ ] **(P2 · 同步域暂缓；2026-09-15 行为审查登记) WS 重连状态机可双连接 / 越权发 `connected`**：
  ① `connect()`（`main/net/wsNetAdapter.ts:30-41`）**不清 pending `reconnectTimer`、不关旧 `ws`**
  就新建连接 → 用户重试 / 服务器不可达时**双连接 / 重连风暴**；② `openSocket` 里 `ws.on('error')`
  已 `reject(err)` 向调用方报失败，但随后的 `close`（:138-144）因 `explicitlyClosed` 仍为假而
  `scheduleReconnect()` → 适配器**在后台静默重试并稍后发出 `connected`**（调用方以为失败、UI 却变已连）。
  处置：`connect()` 入口先 `disconnect()` 式清场；失败路径置"本次连接已作废"标志，close 不再续连。
- [ ] **(P2 · 同步域暂缓；2026-09-15 行为审查登记) REST 无超时**：`request()`
  （`main/net/wsNetAdapter.ts:65-97`）与 `ensureMemberToken()`（:99-109）的 `fetch` **无 timeout /
  无 AbortSignal** → 服务器挂死时 create/join/refresh 按钮永久卡住。处置：`AbortSignal.timeout()` +
  超时归到 `server-error`。附带：`connect()` 失败后 `memberToken` 仍保留（:35-39 先赋值后 `openSocket`），
  后续 `request()` 会带陈旧 token。

- [ ] **(P3) 协议形状收拢**：信封 type 字符串与 payload 形状散落在 RoomSession 的
  switch-case 与 REST body 里 → 抽 `core/domain/protocol.ts`（常量 + 归一化），
  防止"各处自行解释"的腐化（与定位系统同类问题）
- [ ] **(P3) JoinResult 类型重复**：domain 与 RoomSession 各一份、reason 枚举不一致 → 收敛 domain
- [ ] **(P3) emitLocation 绕过节流**：手动路径直接 send，与 onRenderLocation 的 300ms 节流不一致 → 统一走 throttleSend
- [ ] **location-updated 同位 UI（跟随模式）**：RoomFeature 消费 ReaderFeature 的跳转回调（FEATURES §9）
- [ ] **client 管理界面**：admin 操作（删房间/删副本）在客户端完成——协议已支持（REST + admin token）

### 标准化与书籍身份

- [ ] **「标准化」落地**：`WorkIdentity`（protocol + code，如 ISBN）+ ISBN 校验 +
  房间功能内的入口（短期手填、远期 OCR）。口径见 `docs/ARCHITECTURE.md` §1；
  与「书籍标题仍来自文件名」同源（要不要用解析出的 title 覆盖显示，等这条一起定）
- [ ] **(P2) 书籍标题仍来自文件名**：`FingerprintService.extractMetadata` 只去扩展名，故书名带
  "(作者)" 之类后缀（如 `高级运动营养学（第2版） (丹·贝纳多特)`，而 EPUB 的 `dc:title` 是干净的）。
  **封面已由 `CoverQueue` 解决**（`IRenderService.getMetadata()` 已在用）；剩下的是：要不要用解析出的
  title 覆盖显示标题（一行代码），还是留给「标准化」确认 —— 作者/出版社按决策**不采集**，ISBN 归标准化
- [ ] **OCR（ISBN 提取）是否进 v1**：`IOcrService` 草案；技术路线 PP-OCRv5 纯本地
  （OCR-buddy 路线：`ppu-paddle-ocr` + `onnxruntime-web`、PP-OCRv5、纯本地、MIT）

### 渲染层风格与验收

- [ ] **(专项) 深色主题显示问题**（2026-09-09 记录，用户指定单独立项）：暗色下多处文字可见性有问题。
  ① 视图切换动画的"深色块"与页面底色几乎同色 → **已修**（`--flash-bg` 改为反色块）；**其余动效待复核**。
  ② **「阅读器内部分文字被吞没」已销案（2026-09-11）**：隔离测试（同书同冷态、浅/深背靠背）显示两侧
  `bodyHtml=704`、`正文textContent=109` **完全一致** —— 症状来自把 `innerText`（排版相关，同模式两次运行
  给过 70/109）当"正文长度"用；自检已改 `textContent` 判定，并新增「applyTheme 前后正文不许变」的回归断言。
  **剩余 = 全 token 对比度审计**（四套取向 × 全部文字色，含 muted/状态色/负片）+ 阅读页四套取向实测；
  结论与规则落 `client/docs/STYLE.md` §5.6。
- [ ] **性能验收标准显式化**（2026-09-12 竞品调研结论）：Electron 阅读器最普遍差评 = 长书翻页卡顿/大书库慢（Koodo 的头号槽点）。把"千页长书键盘连翻不卡、大书库打开不慢"列为显式验收（进 `STYLE.md` §8），参考 Thorium"每书独立 reader 窗口"隔离
- [ ] **渲染层验收断言待补**（`client/docs/STYLE.md` §8）：
  已补（2026-09-11，见 `STYLE.md` §8.1）：深色注入回归（注入前后正文不许变，PDF 跳过）｜注入事实（计算样式）｜滚动条应否出现｜恢复断言（按格式分判据、失败带诊断）。
  仍缺：① 「无边框文字按钮存在性」与「字号白名单扫描」两条断言
  ② grep 断言（`#hex` / `font-family` 只允许出现在 `styles.css`）纳入审查

### 工程与测试设施

- [x] ~~**样式样张整页黑屏**（用户 2026-09-16 报："`npm run style` 启动是黑屏的"）~~ **已修（2026-09-16）**：
  根因 = **数据层换代后样张没跟着走** —— v0.4.0 把 `BookRecord` 拆成 `EditionRecord` + `ReadingState`，
  样张仍按旧形状给 mock，且 `LibraryToolbar` 的必填 prop `crumbs` 缺失 → **渲染期抛错 → React 卸掉整棵树**，
  只剩 body 的 `--bg`（一片黑）。⚠ **不是"样式不对"，是"页面没了"**，而样张是 `STYLE.md` §8.0 的
  **效果确认第一手段** → 基线验收等于瞎了一半。修法 = `tools/style-gallery/main.tsx` 对齐 v0.4.0
  （`EditionRecord` + `ReadingState` 分开给、补 `readingState`/`onOpenManager`/`crumbs`、
  `TocPanel`/`ReaderControls` 两段 demo 换成**真实的 `ReaderRail` 组合** 4 态）。
  **防线已固化**：① `STYLE.md` §8.0 增"改数据层 / 组件 props 后必跑 `typecheck:preview`"（本次 15 处漂移它一个不漏）
  ② 新增 `tools/style-gallery/smoke.cjs`（Electron 真载入 + 量 DOM + 抓控制台；判据 = 有面板 + 有文字 + 无页面错误，
  实测 `panels=18 / textLen=3831 / pageErrors=[]`）③ 新增 `npm run typecheck:all`；`client/.gitignore` 忽略其 userData。
  **剩余（低优）**：把 ② 接进常态检查（现在仍是手动两步：先 `npm run style` 再跑探针）。
- [ ] **打包产物跑自检时不自退（2026-09-11 实测）**：用**打包后的 exe**（`release/win-unpacked/TuRead.exe`）
  加 `TUREAD_DEV_BOOK` 跑自检，应用**不会自己退出**（开发态 `npm run dev` 正常退出）→ 拿不到
  `[TUREAD-TEST-*]` 文本与退出码，只能靠 userData 落盘间接判断（已用它确认：导入 → 封面 → 打开阅读器 →
  记位置整条链在打包产物里成立）。候选原因：打包后 `console-message` 的转发路径与开发态不同，或
  180s 超时那次 `app.exit` 未生效。修好它才能在发行版上做"断言级"验证。
- [ ] **(发行物缺件，对外发行前必修) 许可文件随包**（2026-09-11 记录，用户当时定"先不管、登记到这里"）：
  ① 仓库**没有 LICENSE 文件**（全史都没有），而 README/MAP 都声明"以 AGPL-3.0 开源"
  ② **OFL 全文未随包**（源流明体要求保留许可声明；`assets/fonts/NOTICE.md` 已自标"待补"）
  ③ 第三方声明（kookit AGPL / Electron / React / pdfjs / Tailwind… 的版权与许可）也应随发行物。
  做法：取 **AGPL-3.0 与 OFL 1.1 官方全文**（走本机代理用 Node 取，**不要手抄**）→ 仓库根放 `LICENSE`，
  字体目录放 `OFL.txt`，让 `electron-builder` 把它们打进发行物（`extraResources` 或 `files`）。
  ⚠ 现状：`npm run dist` 产出的**免安装便携版**（v0.1.15 起的发行形态，用户 2026-09-14 定）**不含许可文件**
  —— 内部/自用可以，**对外发行前必须补**（**正式对外发版 = 出 NSIS 安装包**，许可随包同步生效）。
- [ ] **(P2) 「重开书偶发空白」（2026-09-11 dev 自检实测，间歇 ~1/3 次）**：自检的「恢复」断言
  （`closeReader()` → 点侧边栏「阅读」）有时回来后**没有正文**：实测证据 = 宿主 `#page-area` 节点**没换**
  （`节点同一=y`）、面板在（`stage=yes`）、但 **iframe 数 0**、`render.getPosition()` 全零
  （= 适配器里 `rendition` 为 null，即这次重开**根本没走到 renderTo**）；同一配置同一数据重跑即 ok，
  且**无任何 renderer 报错**。候选原因：① `AppShell.navigate('reader')` 里 `books.getLastRead()` 的
  异步分支与 `closeReader` 竞态（该分支**没有 catch**）② `KookitRenderAdapter.open` 的代次守卫把重开作废
  （已在命中处补 `console.warn`，下次复现即可判定）。
  **2026-09-13 进展**：候选①的 catch 已补（失败会 pushLog + console.warn，现形不再静默）；
  导航方法"未打开即抛错"也已落地（pageTurn 失败进日志）——两者下次复现时都有痕迹可判。
  **2026-09-15 复跑证据（v0.4.0 改造后）**：同一台机、同一本书、隔离 userData 连跑 4 次自检
  → **3 次 `恢复=ok`（含 `章节数=98`）、1 次命中**；命中的那次签名与登记**完全一致**：
  `节点同一=y stage=yes iframe=0 适配器位置=章0/count0/pct0`，且应用内日志尾**只有一次「已打开」**
  （= 重开那次根本没走到 `renderTo`，与"候选②代次守卫作废"或"候选①竞态"相符）。
  ⚠ 结论：**间歇性确认成立（约 1/4），非确定性失败**；`lastLocation` 恢复本身工作正常
  （3/3 ok，v0.4.0 已把首次定位从 `record.lastLocation` 改为 `options.lastLocation` 显式传入）。
- [ ] **自检的时序脆弱与目录抖动**（2026-09-11 记录，本轮排查副产品）：
  ① 文字类 **scroll 模式下 `waitScrollSettle` 常吃满 12s**（宿主 `scrollTop` 长时间不停稳）→
  逐章扫描最坏 ~150s，会把"跑得慢"误报成 FAIL（已把 dev 超时 120s→180s 兜底，根因未查）。
  ② **`getChapter()` 同书同模式两次运行给过 0 项 / 7 项**（AZW3 实测）→ 目录挂载线偶发不出现。
  需定位是 kookit 的 TOC 异步构建、还是我们在 `renderTo` 后读得太早（`ReaderFeature` 只读一次）。
- [ ] **kookit 上游跟进机制**（2026-09-12 调研结论）：kookit = 单人内部库（bus factor=1，不发 npm，TuRead 几乎唯一外部消费者），但短期健康（主程序月发 2-3 版重度依赖）。建立"定期跟进上游 + 本地 diff 评审"，保持随时 fork 的预案
- [ ] **(2026-09-13 用户提出) wasm 替代纯 JS 的适用面评估**：原则 = 只用于 **CPU 密集热点**
  （远期候选：OCR 的 PP-OCRv5 + onnxruntime-web、墨迹渲染/抽稀、大书库批量索引）；
  UI/编排/IO 类 JS 代码不值得 wasm 化（收益为零还要付工具链成本）。现状盘点：better-sqlite3
  已是原生模块、指纹哈希只有 192KB 采样、Veil/虚拟滚动均无热点——**当前没有需要 wasm 的瓶颈**，
  按需逐案评估；引入先核许可证与 AGPL 兼容性。
- [ ] **(P2) 导入的整文件 IPC 传输待优化**（封面离屏化余债，2026-09-12）：指纹只哈希 192KB 采样，
  但文件得整个读进渲染层（`IBookPicker.readFile` → ArrayBuffer 过桥）——大文件夹导入慢的根源。
  候选：指纹/入库在主进程直读文件（渲染层只传路径）。
- [ ] **(P3) 自检对"单章书"死等**（2026-09-12 MD 实测暴露）：单章文档（MD/TXT 短文）翻页无下一章，
  扫描循环吃满 waitForTurn(15s)×10 + waitScrollSettle(8s)，180s 超时误报 FAIL——扫描循环应识别
  "位置到末章"提前收尾。
- [x] ~~vitest 引入~~ **已完成**（v0.1.16）—— 单测/探针分工口径（**单测验判据、探针验链路**）与分层守卫见 `docs/STATUS.md` §4 与 `STYLE.md` §8。

- [ ] **(P2 · 2026-09-15 行为审查登记) 离屏解析窗崩溃不自愈，且会把封面负缓存永久污染**：
  ① `main/index.ts` 的 `render-process-gone`（:44-53）只对在途任务快速失败 + 置 `parseReady = false`，
  **不销毁/不重建窗口** → `ensureParseWindow`（:29-31）因 `!isDestroyed()` 返回"已崩溃未销毁"的旧窗；
  之后的 `metadataParseRequest` 走"未就绪 → 塞 `parseQueue`"分支，**而排空只发生在
  `metadataParseReady`**（:71-76，崩溃后再不会来）→ **队列再无人排空**，每次元数据提取都干等 60s 超时。
  ② 真正的杀伤在**叠加效应**：在途任务收到 `解析进程退出`、排队任务收到 60s 超时，而 `CoverQueue`
  **对任何失败一律 `updateBook(id, { coverFailed: true })` 落库**（`CoverQueue.ts:95-99`）
  → **一次崩溃 = 在途那批书永久失去封面，且跨重启存续**（`coverFailed` 无 UI 可清 ——
  "封面维护动作"仍在「书库 UI」组待办）。根因口径：`offscreenMetadataExtractor.ts:9` 自述
  "单任务 60s 超时 reject（调用方 CoverQueue 会标 coverFailed）"，**把瞬态故障与永久故障一视同仁**
  （"该书没有封面"是永久，超时/进程退出是瞬态）。
  处置候选：崩溃即销毁重建 + 排空 `parseQueue`（在途 job 立即失败而非等 60s）；`coverFailed` 改带
  reason（`transient|permanent`），或只对"确认无内嵌封面"落负缓存。
- [ ] **(P3 · 2026-09-15 行为审查登记) 两个队列的 pump 在尾部重置共享批次状态（重入雷）**：
  `CoverQueue.pump`（`CoverQueue.ts:83-122`）先置 `running = false`（:108）再 `emit('done')`（:109），
  **最后**才清 `queue/total/done/ok/failed`（:116-121）；`ImportQueue.pump`（:81-116）同构。
  若某个 `done` 监听里**同步** `enqueue`（enqueue 内部也会 `void this.pump()`）→ 此时 `running` 已是 false，
  第二轮 pump **立刻起跑**；返回后外层继续清零 → **新批次被整个抹掉 + 计数错乱 + 多发一次空 done**
  （比"丢条目"更重）。
  现状**不可达**：`covers.enqueue` 只在 `imports` 的 done 回调里调（`LibraryFeature/index.tsx:294→302`），
  两队列都只在**彼此**的 done 里交叉入队 → 属**潜伏雷**。
  处置：批次重置移到 `emit('done')` **之前**，或给批次加代次令牌防交错。
- [ ] **(P2 · 安全 · 2026-09-15 行为审查登记) IPC 桥的信任边界过宽（不该把上游细节当前提）**：
  ① `preload/index.ts:15` 暴露的是**泛化** `invoke(channel, payload)` —— **无 channel 白名单**，
  渲染层可调任意已注册通道（比"暴露了 fs:read-file"更宽）；② `main/ipc.ts` 的 `fsReadFile`（:134-137）
  与 `fsListDirectories`（:126-132）**无路径收敛、无扩展名约束**，接受任意绝对路径；
  ③ **反差**：同文件 `fsShowInFolder`（:87-92）是**收敛的**（只接受 `libraryId` → 引导文件里的路径）
  —— 正确范式已在代码里，只是没铺开。
  ④ 可利用性取决于 kookit 的 iframe sandbox（文字类禁脚本 → 链路堵；**PDF 不设 sandbox**，见
  「渲染与阅读器」组同名条目）—— 该前提是**上游实现细节，不是我们的保证**。
  处置候选：preload 按通道白名单转发（或收窄为具名方法）；`fs:*` 限定在库根/导入来源范围内 +
  电子书扩展名（`EBOOK_EXT_SET` / `collectEbooks` 的判据可直接复用）。
- [ ] **(P2 · 2026-09-15 行为审查登记) 单测覆盖缺口（脆弱点恰好都在无覆盖区）**：
  现有自动化 = `domain/anchor` 语义（34）+ 分层依赖守卫（8）＝ 42 断言，**全部是纯逻辑**；
  **无覆盖**：WS 重连状态机、`RoomSession`、双队列（Cover/Import）、SQLite 迁移、全部渲染行为
  —— 恰是上面各条脆弱点所在区域。dev 探针（`selfCheck` / `library` / `note` / `pdfWidth` /
  `pagedInteract`）覆盖真实链路，但需 env flag 无头触发、**不进常态 CI**。
  处置：按"回归成本 × 脆弱度"起步 —— 双队列与 SQLite 迁移是纯逻辑、可无书跑，优先补。

### 远期 · 官方插件

- [ ] **官方插件首个样例**：用 OCR/翻译面板验证 Feature 容器扩展性（原 FEATURES §9 下一步）
- [ ] **TTS 有声阅读（远期，2026-09-09 群聊定案要做）**：**插件化**实现、排远期，**前置条件 = 插件系统完善之后再做**。
  届时形态 = 官方插件（进 ServiceContainer 的能力服务 + 可选 Feature 面板）；朗读位置锚点复用定位系统
  （`BookLocation`，`CONTRACTS.md` §2.1 已预留"将来的 TTS"用例）。⚠ 与「跨端·语音通话（远期挂起）」是两回事：
  TTS = 本地朗读，语音通话 = 房间内实时语音
- [ ] **AI 解析注释（远期，2026-09-09 群聊定案要做）**：对笔记/注释做 AI 理解并在注释界面呈现解析结果
  （群聊中李子的"做个 ai 解析"方案）。**前置 = ① 笔记/划线落地（高优 1）② 插件系统完善后**。定位为官方插件

## server

- [ ] **房主权限（转让）**：`owner_token` 转让给房内另一成员（协议待设计：`POST /rooms/{id}/transfer`）；
  删房已 v0.2.0 完成
- [ ] **部署形态**（VPS/Docker）——不影响代码，仅配置；之后补 `cmd/smoke` 通电检查
- [ ] **服务器地址运维细节**（公网 IP / 端口映射 / HTTPS）——与服务器负责人讨论
- [ ] **用户系统（剩余）**：昵称/bio 编辑、`limited` 角色语义、token 加盐哈希 —— **搁置**（2026-08-29 决定）

## 跨端

- [ ] **书籍来源**：仅本地导入 vs 服务器共享书库
- [ ] **账号体系**：游客昵称 vs 注册——影响 `RoomMember.id` 语义；服务端签发 token 已铺路
- [ ] **语音通话（远期挂起，2026-08-31 定案）**：SFU 中继（服务器只转发音频），编解码在客户端，
  不加密；同步走 WS:8080，媒体另起 UDP/RTP；client v1 落地后再评估

## 产品与发行（非技术）

> 2026-09-15 用户定调：这些是**产品/发行**层面的事，不是代码待办；登记在此以免下次会话又当技术项讨论。

- [ ] **Pro 版 = 功能分层，不是订阅**（2026-09-15 用户定）：用户原话"**pro 只是功能上 pro，不是说订阅**"
  —— **不做订阅制**；愿意支持的用户走**捐赠**（爱发电）。功能候选：
  **笔记迁移到更好的 edition**（建模成本 ≈ 0：靠现成的 `TextAnchor` + `remeanchorAnchor`）、
  **为没有目录的书做目录**（需 `edition_toc` 存储位，见文首 ★ 块第 ⑧ 步）、**导出库包**（F6）。
  ⚠ 待定 = **边界与授权方式**：哪些功能进 Pro、是否需要账号/许可校验（会**反向影响登录设计**，故不宜拖太久）。
  ⚠ 许可约束：kookit 为 **AGPL-3.0** → 发行物必须随包许可文件（已登记在「工程与测试设施」组）；
  Koodo 自身即"免费开源 + 专业版"的先例，路线可行。
- [ ] **捐赠入口**（2026-09-15 用户定）：**爱发电** —— 待做的是把入口放进应用/仓库（形态待定）。
- [ ] **服务器开源 + 可自建**（2026-09-15 用户定，作为**成本策略**）：`server/` 本就随仓库开源
  （AGPL-3.0），用户可自建 → **社区自建会摊薄我们的维护成本**，因此"维护服务器成本"**不构成**
  订阅收费的理由。连带影响：`server/docs/OPS.md` 是面向自建者的指引（现已可用）；
  **不需要**做官方托管服务的账号/计费体系。
